---
phase: 01-merge-action-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - web/src/api/client.ts
  - web/src/types/api.ts
  - web/src/hooks/queries/useTaskWorktreeMergeState.ts
  - web/src/hooks/mutations/useMergeTaskWorktree.ts
  - web/src/hooks/useSSE.ts
  - web/src/components/SessionChat.tsx
  - web/src/routes/projects/task-session-chat.tsx
autonomous: true
requirements:
  - ACTION-01
  - ACTION-02
user_setup: []
must_haves:
    truths:
      - Merge runtime status survives refresh or reconnect and remains visible on the task or chat surface.
      - Detailed running output lives in normal conversation or tool-call messages rather than ephemeral local-only chips.
      - The user sees compact queued, waiting, running, retrying, blocked, succeeded, or canceled state plus cancel or next-step affordances where appropriate.
      - A short latest-note or summary can surface the current attempt or retry state without replacing the transcript as the detailed source of truth.
    artifacts:
      - web/src/components/SessionChat.tsx
      - web/src/hooks/queries/useTaskWorktreeMergeState.ts
      - web/src/hooks/useSSE.ts
      - web/src/types/api.ts
    key_links:
      - Web status rendering consumes durable merge runtime state from hub APIs or SSE rather than component-local merge events.
      - Success, retrying, and blocked summaries stay compact while the transcript carries detailed output.
---

<objective>
Surface the Phase 1 merge runtime durably in web or task views and retire ephemeral merge-only UI state as the source of truth.

Purpose: Complete the user-facing side of the merge runtime so remote control or reconnect behavior matches the product goal.
Output: Compact durable merge status in the task or chat UI, refreshed through normal API or SSE paths, with detailed running output living in the conversation itself.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-merge-action-runtime/01-CONTEXT.md
@.planning/phases/01-merge-action-runtime/01-RESEARCH.md
@.planning/phases/01-merge-action-runtime/01-01-PLAN.md
@.planning/phases/01-merge-action-runtime/01-02-PLAN.md
@web/src/components/SessionChat.tsx
@web/src/hooks/queries/useTaskWorktreeMergeState.ts
@web/src/hooks/mutations/useMergeTaskWorktree.ts
@web/src/hooks/useSSE.ts
@web/src/api/client.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ephemeral merge event state with durable runtime status rendering</name>
  <files>web/src/components/SessionChat.tsx, web/src/hooks/queries/useTaskWorktreeMergeState.ts, web/src/types/api.ts, web/src/api/client.ts</files>
  <action>Stop treating local `mergeEvents` as the primary runtime truth. Render compact merge runtime status from durable hub-backed state and keep the transcript or tool-call messages as the detailed running view. Preserve short success, retrying, and blocker summaries on the task surface.</action>
  <verify>Run `bun run typecheck:web` after the API, type, and UI changes compile cleanly.</verify>
  <done>SessionChat and related task views read durable merge runtime status instead of relying on local-only merge event state.</done>
</task>

<task type="auto">
  <name>Task 2: Add queued, running, retrying, blocked, canceled affordances and cancel control</name>
  <files>web/src/components/SessionChat.tsx, web/src/hooks/mutations/useMergeTaskWorktree.ts, web/src/api/client.ts</files>
  <action>Expose the compact queued, waiting, running, retrying, blocked, succeeded, or canceled lifecycle on the task or chat surface, including the always-available stop or cancel affordance backed by the Phase 1 runtime contract. Keep the UI small and subordinate to the thread rather than building a new action panel.</action>
  <verify>Run `bun run typecheck:web` and confirm the UI state model handles queued, retrying, blocked, and canceled states without regressions.</verify>
  <done>The task or chat surface shows concise durable status, including retry-aware state, and offers cancel or next-step controls where required by the Phase 1 decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Rehydrate merge runtime across SSE and refresh boundaries</name>
  <files>web/src/hooks/useSSE.ts, web/src/routes/projects/task-session-chat.tsx, web/src/components/SessionChat.tsx</files>
  <action>Ensure SSE or query refresh and task-chat reattach flows correctly rehydrate merge runtime state after reload or reconnect. Keep the detailed running narrative inside normal conversation messages and verify the compact task status reappears consistently with the latest short note or attempt state.</action>
  <verify>Run `bun run test:web` if web tests exist; otherwise document the gap and run `bun run typecheck:web` plus a manual refresh or reconnect verification.</verify>
  <done>Refreshing or reopening the task chat preserves compact merge runtime status and does not lose the user’s place in the action flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:web`
- [ ] `bun run test:web` (or documented manual fallback if web tests are not available)
- [ ] Manual refresh or reconnect check confirms compact merge status survives while detailed output remains in the conversation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Merge runtime status is durable and compact outside the transcript
- Detailed running output appears through ordinary message or tool-call flow
</success_criteria>

<output>
After completion, create .planning/phases/01-merge-action-runtime/01-03-SUMMARY.md
</output>
