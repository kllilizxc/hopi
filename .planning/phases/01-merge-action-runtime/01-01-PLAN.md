---
phase: 01-merge-action-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/store/tasks.ts
  - hub/src/store/tasks.test.ts
  - hub/src/store/schemaMigration.test.ts
  - hub/src/web/routes/tasks.merge-error.test.ts
  - hub/src/sync/sessionTaskLink.ts
  - hub/src/sync/sessionCache.ts
  - shared/src/schemas.ts
  - shared/src/types.ts
autonomous: true
requirements:
  - ACTION-01
  - ACTION-02
  - ACTION-03
user_setup: []
must_haves:
    truths:
      - Merge runtime state exists durably outside component-local UI state.
      - Best-usable-session resolution is centralized and deterministic for a task.
      - Task/session linkage updates remain coherent across relink and resume flows.
      - Compact runtime vocabulary includes queue, waiting or approval-pending, running, retrying, blocked, succeeded, canceled, and short latest-note semantics for downstream UI.
    artifacts:
      - hub/src/store/index.ts
      - hub/src/store/tasks.ts
      - hub/src/store/types.ts
      - hub/src/sync/sessionTaskLink.ts
      - shared/src/schemas.ts
    key_links:
      - Task persistence exposes merge runtime state that web and hub can both read.
      - Session relink helper updates both task-facing and session-facing linkage consistently.
      - Retry and waiting-oriented runtime fields are durable even before Phase 2 fills them with full self-healing behavior.
---

<objective>
Establish the durable merge runtime model and central session-resolution primitives Phase 1 needs.

Purpose: Give Merge a stable, replayable runtime foundation before changing how it enters the conversation.
Output: Persisted merge runtime fields/state, centralized best-usable-session resolution, and regression coverage for link/relink behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-merge-action-runtime/01-CONTEXT.md
@.planning/phases/01-merge-action-runtime/01-RESEARCH.md
@hub/src/store/index.ts
@hub/src/store/tasks.ts
@hub/src/store/types.ts
@hub/src/sync/sessionTaskLink.ts
@hub/src/sync/sessionCache.ts
@shared/src/schemas.ts
@shared/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable merge runtime state to task persistence</name>
  <files>hub/src/store/index.ts, hub/src/store/types.ts, hub/src/store/tasks.ts, shared/src/schemas.ts, shared/src/types.ts</files>
  <action>Add the smallest durable task-backed merge runtime shape needed for Phase 1 queued, waiting or approval-pending, running, retrying, blocked, succeeded, and canceled behavior. Include migration-safe store updates, typed read/write paths, short latest-note support, and schema exposure. Do not add a generic custom-action framework or Phase 2/3 execution semantics.</action>
  <verify>Run `bun run typecheck:hub` after the schema/store changes compile cleanly.</verify>
  <done>Task persistence can represent compact merge runtime status durably, including waiting and retry-oriented states, and the shared/task-facing types compile without drift.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize best-usable-session resolution and relink behavior</name>
  <files>hub/src/sync/sessionTaskLink.ts, hub/src/sync/sessionCache.ts, hub/src/store/tasks.ts</files>
  <action>Create or extract one authoritative path for resolving the best usable merge session for a task: prefer the linked session when valid, resume stale-but-recoverable sessions, relink to the best usable session when drifted, and keep task/session metadata coherent. Reuse existing resume behavior rather than inventing a second linkage model.</action>
  <verify>Run `bun run test:hub` with focused coverage for relink/resume/link-drift cases.</verify>
  <done>Merge runtime can ask one helper for the correct session choice and relink behavior is deterministic under stale or drifted session conditions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage around durable runtime and linkage</name>
  <files>hub/src/store/tasks.test.ts, hub/src/store/schemaMigration.test.ts, hub/src/web/routes/tasks.merge-error.test.ts</files>
  <action>Add or update targeted hub-side tests that protect the new runtime fields, migrations, status vocabulary, and relink invariants. Favor narrow deterministic tests over broad end-to-end coverage in this phase.</action>
  <verify>Run `bun run test:hub` and confirm the new task/runtime regression cases pass.</verify>
  <done>There is automated coverage for migration safety, runtime-state persistence, waiting and retry-status vocabulary, and task/session relink invariants.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:hub`
- [ ] `bun run typecheck:hub`
- [ ] Durable merge runtime state survives store round-trips and relink flows in tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a durable merge runtime vocabulary anchored in task/store state
- Best-usable-session resolution is centralized and safe for downstream plans
</success_criteria>

<output>
After completion, create .planning/phases/01-merge-action-runtime/01-01-SUMMARY.md
</output>
