---
phase: 01-merge-action-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/taskSessionService.ts
  - hub/src/sync/sessionTaskLink.ts
  - hub/src/store/tasks.ts
  - hub/src/web/routes/tasks.workflow.test.ts
  - hub/src/sync/taskSessionService.test.ts
  - hub/src/sync/sessionCache.test.ts
autonomous: true
requirements:
  - MERGE-01
  - MERGE-03
  - ACTION-03
user_setup: []
must_haves:
    truths:
      - Clicking Merge starts from the linked task/session context instead of a detached backend-only path.
      - Missing, wrong, or stale session cases resolve according to the locked handoff rules.
      - Busy sessions yield explicit queued, waiting, or approval-pending behavior instead of silent failure or hard interruption.
      - Queued or running merge runtime has an explicit backend/runtime cancel contract for the web layer to call.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/taskSessionService.ts
      - hub/src/store/tasks.ts
    key_links:
      - Merge entry route uses the centralized session-resolution/runtime state from Plan 01.
      - Conversation kickoff is injected through normal session messaging after handoff decisions are resolved.
      - Cancel or stop operations mutate the same durable runtime state rather than bypassing it.
---

<objective>
Route the Merge trigger into a canonical conversation-native kickoff flow with queue and handoff semantics.

Purpose: Make Merge enter the existing task conversation predictably, using normal chat/tool-flow expectations, while staying within Phase 1 scope.
Output: A hub-side merge start path that resolves, starts, relinks, or resumes sessions, queues when busy, injects the short merge request, and exposes explicit cancel semantics at the runtime layer.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-merge-action-runtime/01-CONTEXT.md
@.planning/phases/01-merge-action-runtime/01-RESEARCH.md
@.planning/phases/01-merge-action-runtime/01-01-PLAN.md
@hub/src/web/routes/tasks.ts
@hub/src/sync/taskSessionService.ts
@hub/src/sync/sessionTaskLink.ts
@hub/src/store/tasks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace detached merge entry with canonical runtime kickoff</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/store/tasks.ts, hub/src/web/routes/tasks.workflow.test.ts</files>
  <action>Refactor the Merge entry route so it starts a Phase-1 merge runtime rather than jumping directly into today’s mixed execution branches. Use the Plan 01 durable runtime state, keep scope on entry, handoff, and kickoff, and do not pull full Phase 2 self-healing merge execution into this plan.</action>
  <verify>Run `bun run test:hub` against merge route coverage after the entry path is updated.</verify>
  <done>The merge route creates the correct runtime state and delegates to the conversation kickoff path instead of behaving like a detached workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Implement auto-start, relink, queue, wait, and explicit cancel semantics</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.ts, hub/src/sync/sessionTaskLink.ts, hub/src/sync/taskSessionService.test.ts, hub/src/sync/sessionCache.test.ts</files>
  <action>Implement the locked handoff and busy rules: auto-start when no usable session exists, relink to the best usable session when wrong or stale, resume recoverable sessions, queue while the session is thinking, and wait through pending approvals. Add the explicit backend/runtime stop-cancel path for queued or running Phase 1 merge runtime state so the later web plan can call a real contract instead of inventing one.</action>
  <verify>Run `bun run test:hub` and cover no-session, stale-session, wrong-session, thinking, pending-approval, and cancel-path cases.</verify>
  <done>Merge starts in the right conversation context or enters an explicit queued or waiting state, and the runtime exposes a real cancel path for queued or running actions.</done>
</task>

<task type="auto">
  <name>Task 3: Inject short conversation kickoff messages with minimal orchestration wording</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.ts, hub/src/web/routes/tasks.workflow.test.ts</files>
  <action>Use normal session messaging to inject a short merge request into the conversation, plus one short handoff note only when HOPI had to auto-start, resume, or relink first. Keep the wording close to ordinary agent work and avoid a special action-card-only runtime.</action>
  <verify>Exercise the route in tests and manually inspect the resulting message shape in a task session if needed.</verify>
  <done>The conversation receives a short visible merge request that matches the locked kickoff style and preserves task or thread continuity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:hub`
- [ ] `bun run typecheck:hub`
- [ ] Merge start covers no-session, relink, queued, approval-wait, and cancel cases without leaving the task conversation model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Merge click now enters a canonical conversation-native kickoff path
- Busy, handoff, and cancel behavior are explicit and deterministic
</success_criteria>

<output>
After completion, create .planning/phases/01-merge-action-runtime/01-02-SUMMARY.md
</output>
