---
phase: 06-shared-action-runtime-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/store/types.ts
  - hub/src/store/taskStore.ts
  - hub/src/store/tasks.ts
  - hub/src/sync/sessionTaskLink.ts
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/taskSessionService.ts
  - web/src/types/api.ts
  - hub/src/store/tasks.test.ts
  - hub/src/sync/sessionTaskLink.test.ts
  - hub/src/web/routes/tasks.preview.test.ts
  - hub/src/web/routes/tasks.merge-script.test.ts
  - hub/src/web/routes/tasks.start-session.test.ts
autonomous: true
requirements:
  - ACTION-04
user_setup: []
must_haves:
    truths:
      - Merge, Preview, and Init share one durable runtime envelope for session id, timestamps, retry count, blocker note, and failure fingerprint instead of maintaining three divergent task-runtime contracts.
      - Shared lifecycle states are normalized around one core story, while action-specific terminal states can remain where they preserve user clarity.
      - Store persistence, API payloads, and relink-safe session synchronization all read the same runtime contract so later behavior work does not fork again.
      - Consolidation stays internal-first; no user-visible generic action model is introduced in this plan.
    artifacts:
      - shared/src/schemas.ts
      - hub/src/store/tasks.ts
      - hub/src/sync/sessionTaskLink.ts
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/taskSessionService.ts
      - web/src/types/api.ts
    key_links:
      - Phase 6 starts by stabilizing one shared runtime envelope before transcript/control cleanup so ACTION-04 has a single source of truth.
      - Action-specific labels and terminal states remain allowed on top of the shared envelope to honor the Phase 6 context.
      - Merge/Preview route code and Init service code must converge on shared runtime helpers without a big-bang rewrite.
---

<objective>
Consolidate the shared action runtime contract and persisted state across Merge, Preview, and Init without changing the user-facing action framing.

Purpose: make one durable internal runtime envelope the source of truth for all three built-in actions.
Output: shared status vocabulary, shared runtime persistence/relink helpers, and typed API/task payload alignment.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-shared-action-runtime-consolidation/06-CONTEXT.md
@.planning/phases/06-shared-action-runtime-consolidation/06-RESEARCH.md
@.planning/phases/06-shared-action-runtime-consolidation/06-VALIDATION.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/store/types.ts
@hub/src/store/tasks.ts
@hub/src/sync/sessionTaskLink.ts
@hub/src/web/routes/tasks.ts
@hub/src/sync/taskSessionService.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one shared base runtime contract with action-specific status extension points</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, web/src/types/api.ts</files>
  <action>Refactor the current Merge/Preview/Init runtime definitions into one shared base envelope for common fields and one normalized core lifecycle vocabulary. Keep action-specific terminal states allowed where needed, especially Preview `ready/stopped` and any action-specific success wording expectations. Preserve existing task payload compatibility inside this milestone, but remove duplicated runtime-field definitions so ACTION-04 has one internal contract instead of three hand-maintained copies.</action>
  <verify>Run `bun run typecheck:hub && bun run typecheck:web` after the shared runtime typing compiles cleanly.</verify>
  <done>Shared/runtime typing exposes one reusable envelope for built-in actions while keeping action-specific end states possible.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate runtime persistence, relink, and updater helpers around the shared contract</name>
  <files>hub/src/store/types.ts, hub/src/store/taskStore.ts, hub/src/store/tasks.ts, hub/src/sync/sessionTaskLink.ts, hub/src/store/tasks.test.ts, hub/src/sync/sessionTaskLink.test.ts</files>
  <action>Replace parallel merge/preview/init normalization and relink logic with shared helpers built on the Phase 6 runtime envelope. Preserve task-row storage shape and session-id synchronization guarantees, but stop copying the same persistence/relink rules per action. Cover the shared path with store and relink regressions so future built-in actions can reuse the same helper layer safely.</action>
  <verify>Run `bun run test:hub && bun run typecheck:hub` with assertions for all three actions on persistence round-trip and relink-safe session updates.</verify>
  <done>Durable runtime persistence and relink behavior now come from one helper path instead of three parallel implementations.</done>
</task>

<task type="auto">
  <name>Task 3: Move Merge, Preview, and Init orchestration onto shared runtime builders</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.ts, hub/src/web/routes/tasks.preview.test.ts, hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.start-session.test.ts</files>
  <action>Refactor runtime builder/update usage in Merge route flow, Preview route flow, and Init session-start flow so they all consume the same shared runtime helpers introduced above. Keep the current action-specific orchestration boundaries for now, but eliminate duplicated runtime-state construction and patch logic. Make sure route/service code still exposes action-specific semantics through thin adapters rather than re-encoding lifecycle state separately.</action>
  <verify>Run `bun run test:hub && bun run typecheck:hub && bun run typecheck:web` after migrating the three built-in actions onto shared runtime builders.</verify>
  <done>All three built-in actions derive durable runtime state from the same internal contract, giving later plans one stable platform.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:hub`
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] Merge, Preview, and Init all persist/relink through the same runtime envelope without regressing current action-specific terminal states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ACTION-04 has one stable shared runtime contract across shared/store/hub/web layers
- User-facing product framing remains action-specific even though the internal runtime is consolidated
</success_criteria>

<output>
After completion, create .planning/phases/06-shared-action-runtime-consolidation/06-01-SUMMARY.md
</output>
