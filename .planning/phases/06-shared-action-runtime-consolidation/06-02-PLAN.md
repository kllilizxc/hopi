---
phase: 06-shared-action-runtime-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/taskSessionService.ts
  - hub/src/sync/projectScripts.ts
  - hub/src/sync/autoRunScheduler.ts
  - hub/src/web/routes/tasks.merge-script.test.ts
  - hub/src/web/routes/tasks.preview.test.ts
  - hub/src/web/routes/tasks.start-session.test.ts
  - hub/src/sync/autoRunScheduler.test.ts
  - web/src/components/SessionChat.tsx
  - web/src/components/AssistantChat/HappyThread.tsx
  - web/src/lib/task-init-runtime.ts
  - web/src/hooks/mutations/useMergeTaskWorktree.ts
  - web/src/hooks/mutations/useTaskPreview.ts
  - web/src/hooks/mutations/useStartTaskSession.ts
  - web/src/hooks/mutations/useMergeTaskWorktree.test.tsx
  - web/src/hooks/mutations/useTaskPreview.test.tsx
  - web/src/hooks/mutations/useStartTaskSession.test.tsx
  - web/src/components/SessionChat.test.tsx
autonomous: true
requirements:
  - ACTION-04
  - ACTION-05
  - ACTION-06
user_setup: []
must_haves:
    truths:
      - Direct-result transcript messages, repair prompts, blocker notes, and queue/approval-pending summaries follow one shared product grammar across Merge, Preview, and Init.
      - Busy linked sessions defer the first direct attempt visibly and then auto-run once the session is idle, regardless of which built-in action was triggered.
      - Web task/thread controls reuse one shared action grammar for status, retry, cancel, and blocker summaries while keeping user-facing action names specific.
      - Consolidation removes drift in behavior without flattening away action-specific semantics such as Preview readiness or action-specific success wording.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/taskSessionService.ts
      - hub/src/sync/autoRunScheduler.ts
      - web/src/components/SessionChat.tsx
      - web/src/hooks/mutations/useMergeTaskWorktree.ts
      - web/src/hooks/mutations/useTaskPreview.ts
      - web/src/hooks/mutations/useStartTaskSession.ts
    key_links:
      - ACTION-05 and ACTION-06 are primarily behavior/parity problems, not just type problems, so this plan focuses on shared transcript and queue/control semantics.
      - Existing shared `StatusCard` chrome should stay the visual anchor; this plan normalizes behavior beneath it rather than redesigning the UI.
      - Auto-run, route-triggered actions, and session-start-triggered init must all use the same visible deferral/auto-resume discipline.
---

<objective>
Normalize transcript, queueing, blocker, retry, and control-surface behavior across Merge, Preview, and Init on top of the shared runtime contract.

Purpose: make the three built-in actions feel like one product grammar while keeping action-specific labels and end states.
Output: shared CLI-style transcript/result builders, shared busy-session deferral behavior, and shared web control/status semantics.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-shared-action-runtime-consolidation/06-CONTEXT.md
@.planning/phases/06-shared-action-runtime-consolidation/06-RESEARCH.md
@.planning/phases/06-shared-action-runtime-consolidation/06-VALIDATION.md
@.planning/phases/06-shared-action-runtime-consolidation/06-01-PLAN.md
@hub/src/web/routes/tasks.ts
@hub/src/sync/taskSessionService.ts
@hub/src/sync/projectScripts.ts
@hub/src/sync/autoRunScheduler.ts
@web/src/components/SessionChat.tsx
@web/src/components/AssistantChat/HappyThread.tsx
@web/src/hooks/mutations/useMergeTaskWorktree.ts
@web/src/hooks/mutations/useTaskPreview.ts
@web/src/hooks/mutations/useStartTaskSession.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract one shared transcript/result grammar for built-in actions</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.ts, hub/src/sync/projectScripts.ts, hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.preview.test.ts, hub/src/web/routes/tasks.start-session.test.ts</files>
  <action>Consolidate the action transcript builders so direct success, direct failure, retry, repair prompt, and blocked/manual-step messaging all use one shared CLI-style grammar across Merge, Preview, and Init. Keep action names visible in the resulting text, but stop letting helper location dictate different message shapes. Preserve the key product rule: first direct result message lands in-thread before any repair prompt begins.</action>
  <verify>Run `bun run test:hub` with focused assertions on transcript message ordering, blocked-note format, and action-specific titles staying visible across all three actions.</verify>
  <done>Built-in actions now speak the same transcript language while still sounding like Merge, Preview, or Init rather than a generic backend worker.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize busy-session deferral, approval-pending, and auto-resume behavior</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.ts, hub/src/sync/autoRunScheduler.ts, hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.preview.test.ts, hub/src/web/routes/tasks.start-session.test.ts, hub/src/sync/autoRunScheduler.test.ts</files>
  <action>Extract one shared busy-session/runnable gate so Merge, Preview, and Init all visibly queue when the linked session is busy or awaiting approval, then auto-run the first direct attempt once the session becomes runnable. Preserve action-specific cancelability where it makes sense, but unify the deferral/approval-pending/auto-run discipline so ACTION-06 no longer depends on which action entry path was used.</action>
  <verify>Run `bun run test:hub && bun run typecheck:hub` with parity cases for thinking sessions, approval-pending sessions, auto-resume after idle, and auto-run scheduler consistency.</verify>
  <done>Busy-session handling becomes one product rule shared by all built-in actions instead of a route/service-specific accident.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize shared web status and control semantics without genericizing the UI</name>
  <files>web/src/components/SessionChat.tsx, web/src/components/AssistantChat/HappyThread.tsx, web/src/lib/task-init-runtime.ts, web/src/hooks/mutations/useMergeTaskWorktree.ts, web/src/hooks/mutations/useTaskPreview.ts, web/src/hooks/mutations/useStartTaskSession.ts, web/src/hooks/mutations/useMergeTaskWorktree.test.tsx, web/src/hooks/mutations/useTaskPreview.test.tsx, web/src/hooks/mutations/useStartTaskSession.test.tsx, web/src/components/SessionChat.test.tsx</files>
  <action>Refactor task/thread summaries, retry/cancel behavior, and mutation cache helpers so Merge, Preview, and Init use one shared control grammar and one consistent status-card interpretation of the shared runtime contract. Keep action names and action-specific success wording visible, but stop copying the same cache-update and blocker-summary rules in three separate hooks/builders.</action>
  <verify>Run `bun run typecheck:web && bun run test:web` after web summaries, controls, and cache helpers are normalized.</verify>
  <done>Web task/thread control surfaces feel consistently action-native while being powered by one shared action grammar.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:hub`
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] `bun run test:web`
- [ ] Merge, Preview, and Init all show consistent transcript-visible direct results and visible auto-queue/auto-run behavior without shifting to a generic user-facing action UI
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- ACTION-05 and ACTION-06 now read as one product grammar across the three built-in actions
- Web controls and transcript behavior stay action-specific in wording while sharing one runtime/control model
</success_criteria>

<output>
After completion, create .planning/phases/06-shared-action-runtime-consolidation/06-02-SUMMARY.md
</output>
