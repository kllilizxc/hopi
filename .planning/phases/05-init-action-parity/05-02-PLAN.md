---
phase: 05-init-action-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - hub/src/sync/taskSessionService.ts
  - hub/src/sync/autoRunScheduler.ts
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/projectScripts.ts
  - hub/src/web/routes/tasks.start-session.test.ts
  - hub/src/sync/taskSessionService.test.ts
  - web/src/types/api.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
user_setup: []
must_haves:
    truths:
      - The first init attempt still runs the repo-owned script directly and appends CLI-style output before any repair prompt begins.
      - Failed init keeps the started session alive and routes repair/retry through the same linked session instead of archiving it or silently blocking the task.
      - Retry count, failure fingerprint, blocker summary, and exact manual next step persist durably across repeated init failures.
      - Main task kickoff only continues automatically after init succeeds; blocked init never sneaks task work through early.
    artifacts:
      - hub/src/sync/taskSessionService.ts
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/autoRunScheduler.ts
      - hub/src/web/routes/tasks.start-session.test.ts
      - hub/src/sync/taskSessionService.test.ts
    key_links:
      - Init copies Merge/Preview direct-run-first repair discipline instead of keeping a one-shot recovery prompt.
      - Manual start-session and auto-run scheduler use the same repair/blocker logic so the product contract is identical.
      - Durable init runtime, transcript-visible CLI result text, and kickoff gating all move together; none can stay route-local.
---

<objective>
Give Init real action-runtime behavior: direct CLI attempt first, same-session repair only on failure, bounded retries, blocker discipline, and kickoff continuation only after success.

Purpose: Close the biggest product gap between current init handoff and the Merge/Preview runtime model.
Output: Transcript-visible init result messages, bounded repair/retry loop, repeated-blocker stop behavior, and consistent manual/auto-run failure handling.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-init-action-parity/05-RESEARCH.md
@.planning/phases/05-init-action-parity/05-VALIDATION.md
@.planning/phases/05-init-action-parity/05-01-PLAN.md
@hub/src/sync/projectScripts.ts
@hub/src/sync/taskSessionService.ts
@hub/src/sync/taskSessionService.test.ts
@hub/src/sync/autoRunScheduler.ts
@hub/src/web/routes/tasks.ts
@hub/src/web/routes/tasks.start-session.test.ts
@shared/src/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize direct init result messages and repair prompt contract</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/projectScripts.ts, hub/src/sync/taskSessionService.ts, hub/src/web/routes/tasks.start-session.test.ts</files>
  <action>Extract one canonical init command/report contract reused by direct success, direct failure, and repair prompts. Append transcript-visible CLI summaries with command, working directory, stdout/stderr, and exact failure summary before any repair prompt begins. Keep the happy path low-noise: no repair prompt unless the direct init attempt actually fails.</action>
  <verify>Run `bun run test:hub` with focused assertions on direct init transcript messaging and prompt ordering.</verify>
  <done>Init uses one consistent CLI-style transcript envelope instead of ad hoc route-local messaging.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded same-session init repair and retry control</name>
  <files>hub/src/sync/taskSessionService.ts, hub/src/web/routes/tasks.ts, hub/src/sync/taskSessionService.test.ts, hub/src/web/routes/tasks.start-session.test.ts, web/src/types/api.ts</files>
  <action>After a failed direct init attempt, send one repair prompt into the linked session, wait for that session turn to finish, rerun the direct init command, and persist retry count + failure fingerprint on `initRuntime`. Reuse Preview-style repeated-blocker handling: stop when the same blocker repeats or the next step truly requires manual/out-of-sandbox intervention, and persist one exact manual next step. Do not send the main kickoff prompt until init succeeds.</action>
  <verify>Run `bun run test:hub && bun run typecheck:hub` with cases for retry success, repeated blocker stop, exact manual-step text, and no premature kickoff prompt.</verify>
  <done>Init failures become observable same-session repair work with bounded retry logic instead of one-shot handoff text.</done>
</task>

<task type="auto">
  <name>Task 3: Align auto-run scheduler with the same init repair/blocker contract</name>
  <files>hub/src/sync/autoRunScheduler.ts, hub/src/sync/taskSessionService.ts, hub/src/sync/taskSessionService.test.ts</files>
  <action>Make auto-run scheduler preserve the started session and durable init runtime when init fails, rather than archiving the session and blocking the task immediately. Ensure auto-run uses the same direct attempt, repair prompt, retry, success, and blocked-state rules as manual start-session so users never see two different init products.</action>
  <verify>Run `bun run test:hub` and cover manual-vs-auto-run parity for failed init, linked session survival, and blocked outcomes.</verify>
  <done>Manual and auto-started task sessions share the same init runtime behavior and failure recovery contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run test:hub`
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] Direct init failures append CLI result text before repair, repeated blockers stop with an exact manual next step, and kickoff waits for init success
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Init failure handling now matches the direct-run-first, transcript-visible, bounded-retry product story from Merge/Preview
- Auto-run and manual session start no longer diverge on init failure behavior
</success_criteria>

<output>
After completion, create .planning/phases/05-init-action-parity/05-02-SUMMARY.md
</output>
