---
phase: 05-init-action-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/store/tasks.ts
  - hub/src/store/tasks.test.ts
  - hub/src/store/schemaMigration.test.ts
  - hub/src/sync/taskSessionService.ts
  - hub/src/sync/autoRunScheduler.ts
  - hub/src/web/routes/tasks.ts
  - hub/src/web/routes/tasks.start-session.test.ts
  - web/src/types/api.ts
autonomous: true
requirements:
  - INIT-01
  - INIT-03
user_setup: []
must_haves:
    truths:
      - Init execution is represented as a durable task-backed runtime instead of a hidden branch buried inside session bootstrap.
      - Session creation, task linking, init execution, and kickoff continuation are cleanly separated so later repair loops can keep the started session alive.
      - Manual `start-session` and auto-run scheduler paths call the same init orchestration seam and therefore can converge on the same runtime contract.
      - Task payloads and start-session payloads can surface durable init status without forcing the deferred generic multi-action framework.
    artifacts:
      - shared/src/schemas.ts
      - hub/src/store/tasks.ts
      - hub/src/sync/taskSessionService.ts
      - hub/src/sync/autoRunScheduler.ts
      - hub/src/web/routes/tasks.ts
      - web/src/types/api.ts
    key_links:
      - Init reuses Merge/Preview runtime concepts as the reference implementation instead of staying a bootstrap special case.
      - Session spawn success is decoupled from init success so the same started session can survive failure and host repair work.
      - Auto-run and manual start-session converge on one init runtime helper before retry/blocker logic is added in later plans.
---

<objective>
Promote Init from a hidden pre-kickoff script branch into an explicit task action runtime with durable state and shared session-start seams.

Purpose: Give Phase 5 one stable runtime contract before changing repair-loop behavior or UI.
Output: Persisted `initRuntime`, session-start orchestration boundaries, and typed task/start-session payloads that later plans can build on.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-init-action-parity/05-RESEARCH.md
@.planning/phases/05-init-action-parity/05-VALIDATION.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/store/index.ts
@hub/src/store/types.ts
@hub/src/store/tasks.ts
@hub/src/sync/taskSessionService.ts
@hub/src/sync/autoRunScheduler.ts
@hub/src/web/routes/tasks.ts
@hub/src/web/routes/tasks.start-session.test.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable init runtime state to shared schemas and task persistence</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, hub/src/store/index.ts, hub/src/store/types.ts, hub/src/store/tasks.ts, hub/src/store/tasks.test.ts, hub/src/store/schemaMigration.test.ts</files>
  <action>Add the smallest durable `initRuntime` contract needed for Phase 5: merge/preview-style timestamps, retry count, failure fingerprint, latest note, blocked reason, and init-appropriate statuses such as `running`, `waiting`, `retrying`, `blocked`, and `succeeded`. Thread the runtime through task persistence, migrations, and relink-safe session-id normalization. Keep the shape init-specific for now; do not generalize into the deferred shared action manifest.</action>
  <verify>Run `bun run typecheck:hub` after schema/store changes compile cleanly.</verify>
  <done>Task persistence can round-trip durable init runtime state safely and existing rows migrate without data loss.</done>
</task>

<task type="auto">
  <name>Task 2: Separate session spawn from init execution and kickoff continuation</name>
  <files>hub/src/sync/taskSessionService.ts, hub/src/sync/autoRunScheduler.ts, hub/src/web/routes/tasks.ts, hub/src/web/routes/tasks.start-session.test.ts</files>
  <action>Refactor `startSessionFromTask()` so session creation/linking, init execution, and kickoff continuation become explicit stages rather than one hidden bootstrap block. Ensure a started session can remain linked even while init is still running or waiting on later recovery. Make auto-run scheduler and manual start-session route share the same init orchestration seam from the start.</action>
  <verify>Run `bun run test:hub && bun run typecheck:hub` after the seam refactor to confirm manual and auto-run paths still start sessions correctly.</verify>
  <done>Session-start code has one explicit handoff point for init runtime work instead of burying all behavior inside the bootstrap path.</done>
</task>

<task type="auto">
  <name>Task 3: Thread init runtime through task and start-session API surfaces</name>
  <files>hub/src/web/routes/tasks.ts, web/src/types/api.ts, hub/src/web/routes/tasks.start-session.test.ts</files>
  <action>Expose the durable init runtime through task payloads and, where useful, start-session responses so later web work can render typed state without reverse-engineering transcript text. Keep route payloads narrow and brownfield-friendly: enough to explain init state and linked session continuity, but no generic action registry.</action>
  <verify>Run `bun run typecheck:web` after the typed API surface changes compile cleanly.</verify>
  <done>Later plans can consume one typed init runtime contract from task/start-session responses instead of route-local booleans and special-case handoff flags.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] `bun run test:hub`
- [ ] Manual start-session and auto-run both resolve to the same durable init runtime contract on the task record
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Init is task-backed runtime state rather than hidden bootstrap logic
- Session-start seams are explicit enough for later repair/retry work to keep the started session alive
</success_criteria>

<output>
After completion, create .planning/phases/05-init-action-parity/05-01-SUMMARY.md
</output>
