---
phase: 05-init-action-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - web/src/api/client.ts
  - web/src/types/api.ts
  - web/src/hooks/mutations/useStartTaskSession.ts
  - web/src/routes/projects/task-workbench.tsx
  - web/src/components/AssistantChat/HappyThread.tsx
  - web/src/components/SessionChat.tsx
  - web/src/hooks/useSSE.ts
  - web/src/test/renderWithProviders.tsx
  - web/src/hooks/mutations/useStartTaskSession.test.tsx
  - web/src/components/SessionChat.test.tsx
autonomous: true
requirements:
  - INIT-02
  - INIT-03
user_setup: []
must_haves:
    truths:
      - Task and session UI surfaces show durable init runtime summaries from task data instead of relying on ephemeral start-session toast text.
      - Running, retrying, blocked, and succeeded init states survive refresh/reconnect and remain understandable without rereading hidden bootstrap behavior.
      - Start-session cache updates, task invalidation, and session/task re-entry all stay aligned with the same persisted init runtime contract.
      - Blocked init shows the persisted blocker/manual-step summary clearly enough that users know why task work did not continue.
    artifacts:
      - web/src/hooks/mutations/useStartTaskSession.ts
      - web/src/routes/projects/task-workbench.tsx
      - web/src/components/SessionChat.tsx
      - web/src/components/AssistantChat/HappyThread.tsx
      - web/src/types/api.ts
    key_links:
      - Init runtime UI should mirror the proven Merge/Preview banner style rather than inventing a third presentation model.
      - Web uses durable `task.initRuntime` as primary truth and only layers additional live/session context when helpful.
      - Start-session mutation results, task caches, and SSE invalidation must stay aligned so task workbench and session chat do not drift.
---

<objective>
Surface Init runtime parity across task/session UI with durable summaries, reconnect-safe visibility, and cache-aware start-session updates.

Purpose: Finish the user-facing half of Init parity so task startup no longer feels like hidden bootstrap automation.
Output: Typed web init runtime exposure, durable task/session banners, start-session cache updates, and focused web coverage for re-entry behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-init-action-parity/05-RESEARCH.md
@.planning/phases/05-init-action-parity/05-VALIDATION.md
@.planning/phases/05-init-action-parity/05-01-PLAN.md
@.planning/phases/05-init-action-parity/05-02-PLAN.md
@web/src/api/client.ts
@web/src/types/api.ts
@web/src/hooks/mutations/useStartTaskSession.ts
@web/src/routes/projects/task-workbench.tsx
@web/src/components/AssistantChat/HappyThread.tsx
@web/src/components/SessionChat.tsx
@web/src/hooks/useSSE.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread init runtime through web types, start-session mutation, and task caches</name>
  <files>web/src/api/client.ts, web/src/types/api.ts, web/src/hooks/mutations/useStartTaskSession.ts, web/src/hooks/useSSE.ts</files>
  <action>Expose typed init runtime data through the web client and ensure `useStartTaskSession()` updates task/session caches consistently when a session starts with init running, blocked, or already succeeded. Reuse the cache/SSE discipline from Merge/Preview; do not add a separate local init state machine.</action>
  <verify>Run `bun run typecheck:web` after typed client/cache changes compile cleanly.</verify>
  <done>Web has one cache-aware start-session surface for durable init runtime state instead of relying on generic session-start success assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Render durable init summaries in task workbench and task-aware chat</name>
  <files>web/src/routes/projects/task-workbench.tsx, web/src/components/SessionChat.tsx, web/src/components/AssistantChat/HappyThread.tsx</files>
  <action>Render compact init runtime summaries using the same visual weight as Merge/Preview: running, retrying, blocked, and succeeded notes, with blocker/manual-step detail when init stops task progress. Keep the success path quiet and useful; keep durable status text primary over any transient toast. Make sure task-aware chat and task workbench explain when kickoff is waiting on init completion.</action>
  <verify>Run `bun run typecheck:web` and, if dedicated init UI tests are added in scope, run `bun run test:web`.</verify>
  <done>Users can understand init progress and blockers from durable task/session surfaces instead of treating session start as opaque bootstrap magic.</done>
</task>

<task type="auto">
  <name>Task 3: Rehydrate init status across refresh, reconnect, and task/session re-entry</name>
  <files>web/src/hooks/mutations/useStartTaskSession.ts, web/src/routes/projects/task-workbench.tsx, web/src/components/SessionChat.tsx, web/src/hooks/mutations/useStartTaskSession.test.tsx, web/src/components/SessionChat.test.tsx</files>
  <action>Ensure reload, SSE reconnect, or reopening the task/session view restores durable init summaries from task data. Add focused web tests for cache update and re-entry behavior, and keep a manual browser companion check for kickoff timing and blocked-state clarity. Never require the user to reread old transcript messages just to know why init is still running or blocked.</action>
  <verify>Run `bun run typecheck:web` and `bun run test:web`; keep a manual refresh/reconnect check as the companion product-feel gate.</verify>
  <done>Init runtime remains visible and understandable across web re-entry boundaries instead of vanishing after the initial start-session moment.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:web`
- [ ] `bun run test:web`
- [ ] Manual refresh/reconnect check confirms init summaries rehydrate from durable task state
- [ ] Blocked init and delayed kickoff remain understandable from task/session UI without rereading hidden bootstrap transcript
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Init status is durable and understandable across task workbench and session chat surfaces
- Start-session UX now reflects the same task-backed action-runtime story already established for Merge and Preview
</success_criteria>

<output>
After completion, create .planning/phases/05-init-action-parity/05-03-SUMMARY.md
</output>
