---
phase: 04-preview-action-parity
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - web/src/api/client.ts
  - web/src/types/api.ts
  - web/src/lib/query-keys.ts
  - web/src/hooks/queries/useTaskPreviewState.ts
  - web/src/hooks/mutations/useTaskPreview.ts
  - web/src/hooks/useSSE.ts
  - web/src/components/AssistantChat/HappyThread.tsx
  - web/src/components/SessionChat.tsx
autonomous: true
requirements:
  - PREVIEW-03
  - PREVIEW-04
user_setup: []
must_haves:
    truths:
      - Web preview status rendering reads durable preview runtime as the primary truth instead of relying on component-local preview event chips.
      - Ready or running or retrying or blocked or stopped preview state survives refresh and SSE reconnect, while detailed output remains subordinate to the thread or live log panel.
      - Retry, stop, and cancel controls call real hub contracts and keep task plus preview caches consistent.
      - Task-aware thread surfaces show the same retry count, blocker summary, and manual next step text that hub persists durably.
    artifacts:
      - web/src/components/SessionChat.tsx
      - web/src/components/AssistantChat/HappyThread.tsx
      - web/src/hooks/queries/useTaskPreviewState.ts
      - web/src/hooks/useSSE.ts
      - web/src/types/api.ts
    key_links:
      - Web consumes `task.previewRuntime` plus live preview status together, not ephemeral local event arrays as the action source of truth.
      - Query keys, optimistic mutation updates, and SSE invalidation stay aligned with the Phase 4 runtime contract so task and thread surfaces do not drift.
---

<objective>
Surface Preview runtime parity cleanly in the web task or thread UI: durable banner, durable controls, and reconnect-safe rehydration.

Purpose: Finish the user-facing half of Phase 4 so Preview status and blockers remain understandable without rereading hidden route behavior.
Output: Typed web preview hooks, durable preview banner and controls, cache or SSE rehydration, and task-thread surfacing that matches hub runtime state.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-preview-action-parity/04-01-PLAN.md
@.planning/phases/04-preview-action-parity/04-02-PLAN.md
@web/src/api/client.ts
@web/src/types/api.ts
@web/src/lib/query-keys.ts
@web/src/hooks/useSSE.ts
@web/src/components/AssistantChat/HappyThread.tsx
@web/src/components/SessionChat.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed preview query and mutation hooks plus cache keys</name>
  <files>web/src/api/client.ts, web/src/types/api.ts, web/src/lib/query-keys.ts, web/src/hooks/queries/useTaskPreviewState.ts, web/src/hooks/mutations/useTaskPreview.ts, web/src/hooks/useSSE.ts</files>
  <action>Create typed preview query and mutation helpers that combine durable `task.previewRuntime` with live `/preview` telemetry, then keep query keys and SSE invalidation aligned with that contract. Reuse the merge web patterns where they fit, but keep the hook surface preview-specific and minimal.</action>
  <verify>Run `bun run typecheck:web` after the typed client or hook changes compile cleanly.</verify>
  <done>Web has one reusable preview state hook and one reusable preview action hook instead of ad hoc SessionChat-only fetch and mutation code.</done>
</task>

<task type="auto">
  <name>Task 2: Replace local preview event truth with a durable status banner and controls</name>
  <files>web/src/components/AssistantChat/HappyThread.tsx, web/src/components/SessionChat.tsx, web/src/hooks/mutations/useTaskPreview.ts</files>
  <action>Retire `previewEvents` as the primary state model. Render a compact preview runtime banner similar in weight to Merge status: ready URL when available, running or retrying or blocked or stopped summary, retry count, blocker or manual-step detail, and stop or retry controls backed by real hub endpoints. Keep live preview logs secondary and only when useful.</action>
  <verify>Run `bun run typecheck:web` and, if a dedicated preview web test file is added in-scope, run `bun run test:web`; otherwise keep a manual companion check for ready, retrying, blocked, and stopped banner/control states.</verify>
  <done>SessionChat and HappyThread show durable preview status and controls instead of ephemeral event chips deciding the action state.</done>
</task>

<task type="auto">
  <name>Task 3: Rehydrate preview state across refresh, reconnect, and task-thread re-entry</name>
  <files>web/src/hooks/queries/useTaskPreviewState.ts, web/src/hooks/useSSE.ts, web/src/components/SessionChat.tsx</files>
  <action>Ensure refresh, SSE reconnect, or reopening the task thread restores the preview banner and any active ready or running or retrying state from durable runtime plus live preview status. Keep detailed CLI output in the thread or log panel, but never let component-local reset hide the durable blocker or ready story.</action>
  <verify>Run `bun run typecheck:web` and manually verify refresh or reconnect behavior; add `bun run test:web` only if this plan introduces dedicated preview web tests in scope.</verify>
  <done>Preview status and controls survive web re-entry boundaries instead of disappearing with component-local state resets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:web`
- [ ] `bun run typecheck:web`
- [ ] Manual refresh or reconnect check confirms preview banner and controls rehydrate from durable runtime
- [ ] If new preview web tests are added in scope, `bun run test:web`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preview status is durable and understandable across task-aware thread UI and reconnect paths
- Retry or stop controls stay aligned with the hub runtime contract and exact blocker handoff text
</success_criteria>

<output>
After completion, create .planning/phases/04-preview-action-parity/04-03-SUMMARY.md
</output>
