---
phase: 04-preview-action-parity
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - hub/src/sync/projectScripts.ts
  - shared/src/schemas.ts
  - hub/src/store/tasks.ts
  - hub/src/web/routes/tasks.ts
  - hub/src/web/routes/tasks.preview.test.ts
  - web/src/types/api.ts
autonomous: true
requirements:
  - PREVIEW-01
  - PREVIEW-02
  - PREVIEW-04
user_setup: []
must_haves:
    truths:
      - An idle Preview click takes one direct repo-local start attempt first and does not send an agent repair prompt on the clean success path.
      - Busy linked sessions enter queued or approval-pending or waiting state, then auto-run the same direct preview attempt when the session becomes runnable.
      - Failed direct starts and early or late crashes append transcript-visible command plus CLI output context before any same-session repair or retry prompt begins.
      - Retry fingerprints and blocker summaries stop repeated identical failures or real out-of-sandbox blockers with an exact manual next step, and cancel or stop clears any in-flight queued or monitor work.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - hub/src/web/routes/tasks.preview.test.ts
      - hub/src/sync/projectScripts.ts
      - shared/src/schemas.ts
    key_links:
      - Merge direct-run-first control flow is the reference implementation; Preview copies that shape rather than extending the current hidden auto-repair path.
      - Direct attempt, repair prompt, retry count, failure fingerprint, and blocker summary all write the same durable `task.previewRuntime` record.
      - In-memory preview monitors may exist, but they are recreatable from durable runtime plus live preview status and are never the source of truth.
---

<objective>
Give Preview execution parity with Merge: direct run first, transcript-visible repair only on failure, bounded retry control, and clear blocker stop conditions.

Purpose: Make Preview feel like a direct CLI action instead of a hidden preview-specific automation loop.
Output: Canonical direct-start contract, busy-session deferral semantics, same-session repair or retry control, late-crash handling, and blocker or cancel discipline backed by durable runtime state.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-verified-completion-recovery-control/03-01-SUMMARY.md
@.planning/phases/03-verified-completion-recovery-control/03-02-SUMMARY.md
@.planning/phases/03-verified-completion-recovery-control/03-03-SUMMARY.md
@.planning/phases/04-preview-action-parity/04-01-PLAN.md
@hub/src/sync/projectScripts.ts
@hub/src/store/tasks.ts
@hub/src/web/routes/tasks.ts
@hub/src/web/routes/tasks.preview.test.ts
@shared/src/schemas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract one canonical direct preview start contract and busy-session kickoff path</name>
  <files>hub/src/sync/projectScripts.ts, hub/src/web/routes/tasks.ts, hub/src/web/routes/tasks.preview.test.ts</files>
  <action>Define one reusable preview start contract that prefers `.hopi/preview.sh`, otherwise the repo-local `preview` or `dev` or `start` path, with one exact root-path and command summary reused by direct-start result messages and repair prompts. When the linked session is busy, persist queued or approval-pending or waiting runtime state and auto-run the same direct attempt later in that session. Keep the clean success path low-noise: one direct attempt first, no agent repair prompt unless that attempt actually fails.</action>
  <verify>Run `bun run typecheck:hub` after the direct-start and busy-session refactor compiles cleanly.</verify>
  <done>Preview kickoff uses one merge-style direct-run-first scaffold and no longer depends on an eager hidden repair branch.</done>
</task>

<task type="auto">
  <name>Task 2: Convert preview repair into a bounded same-session inspect or fix or retry loop</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/store/tasks.ts, shared/src/schemas.ts, hub/src/web/routes/tasks.preview.test.ts, web/src/types/api.ts</files>
  <action>After a direct start failure or an early crash, append transcript-visible result text with the exact command, root path, error, and useful log tail, then send one repair prompt into the same task session. Persist retry count and failure fingerprint on `previewRuntime`, use the same fingerprint across direct-start and late-crash cases, and stop when the same blocker repeats or the next step truly requires out-of-sandbox or manual intervention. Keep generic custom-action abstractions out of scope.</action>
  <verify>Run `bun run test:hub` with focused assertions on failure transcript, retry count, same-blocker stop, and manual next-step text.</verify>
  <done>Preview failures become visible same-session repair work with bounded retry control instead of opaque background automation.</done>
</task>

<task type="auto">
  <name>Task 3: Reattach late-crash monitoring and stop or cancel behavior to durable runtime</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/web/routes/tasks.preview.test.ts, hub/src/store/tasks.ts</files>
  <action>Ensure late-crash monitoring can be reattached from durable runtime plus live preview status after refresh, repeated start calls, or hub-side re-entry. Stop or cancel must terminate queued kickoff work and any active repair monitor, then move runtime to `stopped` or `canceled` consistently with a short durable note. Avoid hidden auto-retry branches that bypass the transcript or runtime contract.</action>
  <verify>Run `bun run test:hub` and cover queued cancel, ready-to-crash reattachment, and stop-after-retry transitions.</verify>
  <done>Preview monitors and stop controls are durable, restart-safe, and aligned with the bounded repair loop.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] `bun run test:hub`
- [ ] Preview direct success sends no repair prompt, and repeated blockers stop with an exact manual next step
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preview click now feels like a direct repo-local start attempt first, with agent repair only on failure
- Retry or blocker control is durable, same-session, and explicit about manual next steps
</success_criteria>

<output>
After completion, create .planning/phases/04-preview-action-parity/04-02-SUMMARY.md
</output>
