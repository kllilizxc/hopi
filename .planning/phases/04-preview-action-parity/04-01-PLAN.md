---
phase: 04-preview-action-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/store/tasks.ts
  - hub/src/store/tasks.test.ts
  - hub/src/store/schemaMigration.test.ts
  - hub/src/sync/sessionTaskLink.ts
  - hub/src/web/routes/tasks.ts
  - hub/src/web/routes/tasks.preview.test.ts
  - web/src/api/client.ts
  - web/src/types/api.ts
autonomous: true
requirements:
  - PREVIEW-01
  - PREVIEW-03
user_setup: []
must_haves:
    truths:
      - Preview runtime state exists durably on the task record instead of living only in preview RPC output or SessionChat local state.
      - Preview runtime vocabulary stays as close to Merge as practical, but keeps preview-native ready and stopped semantics without introducing a generic action framework.
      - Preview start, status, and stop routes expose one typed durable runtime contract alongside live preview-process data so later web work has a stable API surface.
      - Store migrations plus task-session relink/resume flows keep preview runtime coherent when the active session changes.
    artifacts:
      - shared/src/schemas.ts
      - hub/src/store/tasks.ts
      - hub/src/web/routes/tasks.ts
      - web/src/types/api.ts
    key_links:
      - Preview uses Merge runtime concepts as the reference implementation instead of inventing a second preview-only vocabulary.
      - Task payloads and preview route payloads can both surface the same durable preview runtime before Phase 6 consolidates action contracts.
---

<objective>
Establish the durable Preview runtime contract and route-level API surface Phase 4 needs before changing loop behavior.

Purpose: Give Preview one task-backed source of truth that can survive refresh, reconnect, relink, and later retry logic.
Output: Persisted preview runtime fields, typed shared or web API exposure, and regression coverage for store round-trips plus preview route state updates.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-verified-completion-recovery-control/03-01-SUMMARY.md
@.planning/phases/03-verified-completion-recovery-control/03-02-SUMMARY.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/store/index.ts
@hub/src/store/types.ts
@hub/src/store/tasks.ts
@hub/src/sync/sessionTaskLink.ts
@hub/src/web/routes/tasks.ts
@hub/src/web/routes/tasks.preview.test.ts
@web/src/api/client.ts
@web/src/types/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable preview runtime state to shared schemas and task persistence</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, hub/src/store/index.ts, hub/src/store/types.ts, hub/src/store/tasks.ts, hub/src/store/tasks.test.ts, hub/src/store/schemaMigration.test.ts, hub/src/sync/sessionTaskLink.ts</files>
  <action>Add the smallest durable `previewRuntime` contract needed for Phase 4: merge-style queued or approval-pending or waiting or running or retrying or blocked semantics, plus preview-native ready and stopped outcomes. Thread the new runtime session ID through relink helpers so active-session drift cannot orphan preview state. Keep the field set close to `mergeRuntime` with retry count, failure fingerprint, latest note, and blocked reason. Only add preview-specific fields, such as last ready URL or command summary, if downstream thread or task surfaces truly need them before Phase 6.</action>
  <verify>Run `bun run typecheck:hub` after the schema and store changes compile cleanly.</verify>
  <done>Task persistence can round-trip compact preview runtime state durably and migrations cover existing task rows safely.</done>
</task>

<task type="auto">
  <name>Task 2: Thread preview runtime through task and preview API responses</name>
  <files>hub/src/web/routes/tasks.ts, web/src/api/client.ts, web/src/types/api.ts, hub/src/web/routes/tasks.preview.test.ts</files>
  <action>Centralize helpers that build and update preview runtime from route code, then expose that runtime through the task payload and preview start or status or stop responses in one consistent typed shape. Treat live preview RPC state as process telemetry, not the only source of truth. Keep the API narrow and preview-specific; do not generalize into a multi-action manifest yet.</action>
  <verify>Run `bun run typecheck:web` after the typed client or API surface changes compile cleanly.</verify>
  <done>Routes and client types expose one coherent durable preview runtime contract for later execution and UI work.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage around persistence and route contract</name>
  <files>hub/src/store/tasks.test.ts, hub/src/store/schemaMigration.test.ts, hub/src/web/routes/tasks.preview.test.ts</files>
  <action>Add or update focused tests that cover preview runtime store round-trips, migration safety, start or status or stop route updates, and session-link coherence when `activeSessionId` changes. Favor narrow deterministic coverage over broad end-to-end UI work in this plan.</action>
  <verify>Run `bun run test:hub` and confirm the new preview runtime cases pass deterministically.</verify>
  <done>Automated coverage protects the preview runtime contract before Phase 4 execution-loop behavior lands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run typecheck:web`
- [ ] `bun run test:hub`
- [ ] Preview runtime survives store round-trips and preview API responses expose the same durable state contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preview has a durable task-backed runtime contract instead of preview-only ephemeral state
- Phase 4 execution and web plans can rely on one typed preview runtime surface without introducing the deferred generic action framework
</success_criteria>

<output>
After completion, create .planning/phases/04-preview-action-parity/04-01-SUMMARY.md
</output>
