---
phase: 02-self-healing-merge-loop
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/projectScripts.ts
  - hub/src/web/routes/tasks.merge-script.test.ts
  - hub/src/web/routes/tasks.merge-error.test.ts
autonomous: true
requirements:
  - REPAIR-05
user_setup: []
must_haves:
    truths:
      - Merge recovery stays inside the workspace sandbox and does not silently escape into backend-only retries.
      - Real blockers that need human judgment or out-of-sandbox work produce explicit blocked notes and manual next-step guidance.
      - Hidden merge retry or conflict-resolution helpers are removed, fenced off, or otherwise prevented from becoming the canonical execution path.
      - The Phase 1 durable runtime remains the only user-facing merge runtime contract after cleanup.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/projectScripts.ts
      - hub/src/web/routes/tasks.merge-error.test.ts
    key_links:
      - Script-contract helpers may still exist for shared env knowledge, but actual merge execution remains agent-visible normal tool flow.
      - Blocked outcomes map to concise durable runtime notes that the Phase 1 UI can replay without extra surfaces.
---

<objective>
Harden the merge loop so recovery stays sandbox-local and blockers stop with explicit handoff instead of hidden fallback behavior.

Purpose: Finish Phase 2 by removing brownfield escape hatches that conflict with the product direction and by making blocker discipline explicit.
Output: Cleanup or isolation of hidden backend merge automation, clearer sandbox-only boundaries, and regression coverage for blocker handoff behavior.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-self-healing-merge-loop/02-CONTEXT.md
@.planning/phases/02-self-healing-merge-loop/02-RESEARCH.md
@.planning/phases/02-self-healing-merge-loop/02-01-PLAN.md
@.planning/phases/02-self-healing-merge-loop/02-02-PLAN.md
@hub/src/web/routes/tasks.ts
@hub/src/sync/projectScripts.ts
@hub/src/web/routes/tasks.merge-script.test.ts
@hub/src/web/routes/tasks.merge-error.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or quarantine hidden backend merge automation paths</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/projectScripts.ts</files>
  <action>Audit merge-specific helpers that still perform backend-driven script execution, direct git retry, or background merge loops outside the transcript. Remove them if dead, or fence them off so the canonical Merge path cannot fall back to opaque execution. Preserve shared script-contract knowledge only where it helps the prompt/runtime contract.</action>
  <verify>Run `bun run typecheck:hub` after cleanup or isolation changes compile cleanly.</verify>
  <done>The repo no longer has an attractive hidden merge execution path that conflicts with the conversation-native runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Make sandbox and manual-blocker boundaries explicit</name>
  <files>hub/src/web/routes/tasks.ts</files>
  <action>Ensure Merge prompt text and blocked runtime notes tell the agent to stop only for real manual-judgment or out-of-sandbox blockers, and to summarize the exact next step when it stops. Keep the policy precise without adding Phase 3 loop-detection or repo-truth verification logic.</action>
  <verify>Run `bun run test:hub` with focused blocked-state assertions.</verify>
  <done>Blocked merge outcomes consistently explain whether the blocker is manual judgment, outside-sandbox work, or another non-recoverable condition.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage against opaque retries and weak blocker handoff</name>
  <files>hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.merge-error.test.ts</files>
  <action>Add or update tests that confirm Merge stays in normal conversation semantics, does not silently schedule backend-only retries, and leaves concise blocker guidance when automation must stop.</action>
  <verify>Run `bun run test:hub` and confirm the cleanup and blocker-discipline cases pass.</verify>
  <done>Regression coverage protects the Phase 2 boundary: sandbox-only visible recovery, clear blockers, no opaque retry fallback.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run test:hub`
- [ ] Merge no longer has a hidden backend retry path that can bypass transcript-visible tool calls
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Merge recovery remains sandbox-local and transcript-visible
- Real blockers stop with explicit concise handoff instead of silent backend retries
</success_criteria>

<output>
After completion, create .planning/phases/02-self-healing-merge-loop/02-03-SUMMARY.md
</output>
