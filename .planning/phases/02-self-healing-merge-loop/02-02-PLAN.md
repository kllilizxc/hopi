---
phase: 02-self-healing-merge-loop
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - hub/src/web/routes/tasks.ts
  - shared/src/schemas.ts
  - hub/src/store/tasks.ts
  - hub/src/web/routes/tasks.merge-script.test.ts
  - hub/src/web/routes/tasks.merge-error.test.ts
autonomous: true
requirements:
  - MERGE-04
  - MERGE-05
  - REPAIR-02
  - REPAIR-03
user_setup: []
must_haves:
    truths:
      - After a failed first attempt, the agent is explicitly guided to inspect real tool output and current git or worktree state before deciding the next step.
      - The same task session can continue through inspect, fix, and retry work without bouncing into a detached merge worker.
      - The prompt or runtime notes explicitly allow editing `.hopi/merge.sh` and other workspace files when those changes are the fix.
      - Durable runtime notes remain compact while being retry-aware enough to support the Phase 1 web status surface.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - shared/src/schemas.ts
      - hub/src/store/tasks.ts
      - hub/src/web/routes/tasks.merge-error.test.ts
    key_links:
      - Retry-oriented merge guidance lives in the canonical route prompt or runtime updates, not in a hidden backend retry path.
      - Any new retry note or attempt metadata is durable and compatible with existing `task.mergeRuntime` consumers.
---

<objective>
Teach Merge to behave like a visible in-session inspect/fix/retry loop after the first attempt fails.

Purpose: Make failures actionable for the agent inside the same task conversation instead of ending at a generic blocked state after one attempt.
Output: Retry-aware prompt/runtime behavior, durable short notes that fit the Phase 1 UI, and regression coverage for same-session recovery expectations.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-self-healing-merge-loop/02-CONTEXT.md
@.planning/phases/02-self-healing-merge-loop/02-RESEARCH.md
@.planning/phases/02-self-healing-merge-loop/02-01-PLAN.md
@hub/src/web/routes/tasks.ts
@shared/src/schemas.ts
@hub/src/store/tasks.ts
@hub/src/web/routes/tasks.merge-script.test.ts
@hub/src/web/routes/tasks.merge-error.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the merge prompt into an inspect or fix or retry loop</name>
  <files>hub/src/web/routes/tasks.ts</files>
  <action>Expand the canonical merge prompt so the agent must inspect stdout or stderr, read current git or worktree state, apply repo-local fixes, and retry within the same session before stopping. Keep the wording flexible enough for different repos but explicit enough that the first failed attempt does not end the flow prematurely.</action>
  <verify>Run `bun run typecheck:hub` after the prompt/runtime changes compile.</verify>
  <done>The merge prompt now encodes an in-session repair loop rather than a single-attempt request.</done>
</task>

<task type="auto">
  <name>Task 2: Add durable retry-aware runtime notes without overbuilding Phase 3 logic</name>
  <files>hub/src/web/routes/tasks.ts, shared/src/schemas.ts, hub/src/store/tasks.ts</files>
  <action>Use the existing task merge runtime to preserve concise retry-aware notes, statuses, or attempt metadata only where the route can know them reliably without transcript over-parsing. Do not implement full retry fingerprinting or authoritative success verification in this phase.</action>
  <verify>Run `bun run test:hub` with focused assertions on runtime note and blocked-state behavior.</verify>
  <done>The durable runtime model can summarize retry-oriented progress or failure clearly enough for the existing web surface without pretending to solve Phase 3 verification.</done>
</task>

<task type="auto">
  <name>Task 3: Cover script-fix and workspace-fix recovery expectations</name>
  <files>hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.merge-error.test.ts</files>
  <action>Add or revise targeted route tests that prove the agent is instructed to inspect failure output, may edit `.hopi/merge.sh`, may edit other workspace files, and should retry merge in the same linked session before surfacing a final blocker.</action>
  <verify>Run `bun run test:hub` and confirm same-session repair-loop expectations pass.</verify>
  <done>Regression coverage protects the inspect/fix/retry behavior Phase 2 introduces for merge failures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run test:hub`
- [ ] Merge prompt and runtime notes clearly support inspect/fix/retry behavior in the same session
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Failed merge attempts become visible in-session repair work instead of single-shot blocked outcomes
- Durable runtime notes remain compact and compatible with the Phase 1 web UI
</success_criteria>

<output>
After completion, create .planning/phases/02-self-healing-merge-loop/02-02-SUMMARY.md
</output>
