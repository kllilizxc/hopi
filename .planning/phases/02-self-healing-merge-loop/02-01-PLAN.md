---
phase: 02-self-healing-merge-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hub/src/web/routes/tasks.ts
  - hub/src/sync/projectScripts.ts
  - hub/src/web/routes/tasks.workflow.test.ts
  - hub/src/web/routes/tasks.merge-script.test.ts
autonomous: true
requirements:
  - MERGE-02
  - REPAIR-01
user_setup: []
must_haves:
    truths:
      - Merge kickoff tells the agent exactly how to make the first merge attempt inside normal tool-call flow.
      - The first attempt uses `.hopi/merge.sh` from the correct project root when the script exists, with the repo env contract surfaced to the agent rather than executed by the backend.
      - The merge transcript remains the detailed execution source of truth; no direct backend script run becomes the canonical path.
      - Prompt content and route behavior are locked by focused hub tests so later plans cannot drift back toward opaque execution.
    artifacts:
      - hub/src/web/routes/tasks.ts
      - hub/src/sync/projectScripts.ts
      - hub/src/web/routes/tasks.merge-script.test.ts
    key_links:
      - A single prompt or helper owns the script-first command shape, project-root guidance, and merge env vars.
      - The live merge route reuses that helper while still sending one normal message into the linked task session.
---

<objective>
Turn the Phase 1 conversation kickoff into a deterministic script-first in-session merge attempt contract.

Purpose: Ensure the agent's first move is a normal tool call that runs the repo-owned merge workflow from the right place, with visible output in the transcript.
Output: Structured merge prompt content, consistent repo env guidance, and regression coverage for the script-first kickoff path.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-self-healing-merge-loop/02-CONTEXT.md
@.planning/phases/02-self-healing-merge-loop/02-RESEARCH.md
@.planning/phases/01-merge-action-runtime/01-03-SUMMARY.md
@hub/src/web/routes/tasks.ts
@hub/src/sync/projectScripts.ts
@hub/src/web/routes/tasks.workflow.test.ts
@hub/src/web/routes/tasks.merge-script.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract one canonical script-first merge attempt contract</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/sync/projectScripts.ts</files>
  <action>Define one reusable helper or prompt builder that expresses the first merge attempt as a normal CLI tool call: correct project root, `.hopi/merge.sh` first when present, merge env vars, and visible stdout or stderr expectations. Reuse existing script-contract knowledge from `projectScripts.ts`, but do not execute the script from the backend.</action>
  <verify>Run `bun run typecheck:hub` after the helper or prompt refactor compiles cleanly.</verify>
  <done>The live merge route has one clear script-first prompt contract and no longer relies on vague wording for the first merge attempt.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the live merge route to the structured first-attempt contract</name>
  <files>hub/src/web/routes/tasks.ts, hub/src/web/routes/tasks.workflow.test.ts</files>
  <action>Update the canonical merge kickoff route to use the new script-first prompt contract without regressing Phase 1 session handoff, queued state, or durable runtime behavior. Keep the flow as one message into the linked task session and preserve existing correlation or monitor behavior.</action>
  <verify>Run `bun run test:hub` with focused coverage on merge route kickoff behavior.</verify>
  <done>Merge kickoff still enters the linked task conversation, but the prompt now deterministically prescribes the first in-session merge attempt.</done>
</task>

<task type="auto">
  <name>Task 3: Lock prompt and transcript expectations with regression tests</name>
  <files>hub/src/web/routes/tasks.merge-script.test.ts, hub/src/web/routes/tasks.workflow.test.ts</files>
  <action>Add or update focused tests that assert the merge prompt prefers `.hopi/merge.sh`, includes the right root-path and env guidance, and keeps execution visible through normal conversation semantics rather than direct backend script execution.</action>
  <verify>Run `bun run test:hub` and confirm the script-first kickoff cases pass deterministically.</verify>
  <done>Automated coverage protects the structured first-attempt contract and transcript-visible merge kickoff path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun run test:hub`
- [ ] Merge kickoff prompt clearly prescribes the first script/tool call from the correct project root
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a deterministic script-first kickoff contract in normal conversation flow
- Detailed execution output stays in the transcript rather than hidden backend script runs
</success_criteria>

<output>
After completion, create .planning/phases/02-self-healing-merge-loop/02-01-SUMMARY.md
</output>
