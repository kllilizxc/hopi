---
phase: 01-omc-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - hopi/hub/src/web/routes/omc.ts
  - hopi/hub/src/sync/omc/contextPackBuilder.ts
  - hopi/hub/src/sync/omc/attemptRunner.ts
  - hopi/hub/src/sync/omc/evidenceCollector.ts
  - hopi/hub/src/web/routes/omc.test.ts
  - hopi/OMC-client/src/routes/programs/plan.tsx
  - hopi/OMC-client/src/routes/programs/attempt.tsx
  - hopi/OMC-client/src/components/AttemptInspector.tsx
  - hopi/OMC-client/src/api/client.ts
autonomous: true
requirements:
  - OMC-04
  - OMC-05
  - OMC-07
user_setup: []
must_haves:
    truths:
      - A user can manually start one plan from OMC and see a durable attempt record appear.
      - The first attempt uses a deterministic context-pack shape and is explicitly bound to one plan/worktree.
      - Attempt and evidence visibility are first-class; runtime state does not disappear into hidden session internals.
      - Plan detail remains execution-first, with markdown context secondary to runtime state and actions.
    artifacts:
      - hopi/hub/src/sync/omc/contextPackBuilder.ts
      - hopi/hub/src/sync/omc/attemptRunner.ts
      - hopi/hub/src/sync/omc/evidenceCollector.ts
      - hopi/OMC-client/src/components/AttemptInspector.tsx
      - hopi/OMC-client/src/routes/programs/plan.tsx
    key_links:
      - Phase 1's product promise is not satisfied by a read-only board; the manual plan start path is the proof point.
      - This plan should stop at one runnable attempt path, not expand into a full scheduler or auto-merge system.
      - Execution-first plan detail is a locked design choice and should shape the UI hierarchy directly.
---

<objective>
Add the first runnable OMC interaction: execution-first plan detail, manual plan start, and inspectable attempt/evidence views.

Purpose: prove the first `1 plan -> 1 loop -> 1 attempt` slice without taking on full scheduler complexity.
Output: manual plan-start endpoint, deterministic context-pack generation, first attempt/evidence runtime flow, and attempt inspector UI.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-omc-foundation/01-CONTEXT.md
@.planning/phases/01-omc-foundation/01-RESEARCH.md
@.planning/phases/01-omc-foundation/01-VALIDATION.md
@.planning/phases/01-omc-foundation/01-01-PLAN.md
@.planning/phases/01-omc-foundation/01-02-PLAN.md
@hopi/AGENTS.md
@hopi/cli/src/codex/runCodex.ts
@hopi/hub/src/web/routes/omc.ts
@hopi/OMC-client/src/router.tsx
@hopi/docs/design/factory-mode-ralph-gsd.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build deterministic context-pack and attempt-runner seams for one manual OMC start</name>
  <files>hopi/hub/src/sync/omc/contextPackBuilder.ts, hopi/hub/src/sync/omc/attemptRunner.ts, hopi/hub/src/sync/omc/evidenceCollector.ts, hopi/hub/src/web/routes/omc.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/cli/src/codex/runCodex.ts, hopi/hub/src/web/routes/omc.ts, hopi/hub/src/sync/omc/runtimeStore.ts</read_first>
  <action>Create the first OMC execution seam for a manual plan start. Implement a deterministic `buildOmcContextPack` function in `hopi/hub/src/sync/omc/contextPackBuilder.ts` that includes identity, workspace, planning refs, current objective, acceptance/checks, previous-attempt memory, operating rules, and output contract. Implement a concrete `startOmcPlanAttempt` entry point in `hopi/hub/src/sync/omc/attemptRunner.ts` that is explicitly scoped to one plan/worktree and uses a concrete Codex execution path for the first attempt. Keep the scope narrow: one manual start, one attempt lifecycle, and a concrete `collectOmcEvidence` helper in `hopi/hub/src/sync/omc/evidenceCollector.ts`; do not add scheduler-driven retries or auto-merge behavior.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the OMC start/attempt/evidence backend seams compile and any route/runtime tests pass.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/sync/omc/contextPackBuilder.ts` contains `buildOmcContextPack`
    - `hopi/hub/src/sync/omc/contextPackBuilder.ts` contains `currentObjective`
    - `hopi/hub/src/sync/omc/contextPackBuilder.ts` contains `outputContract`
    - `hopi/hub/src/sync/omc/attemptRunner.ts` contains `startOmcPlanAttempt`
    - `hopi/hub/src/sync/omc/evidenceCollector.ts` contains `collectOmcEvidence`
  </acceptance_criteria>
  <done>Hub code can generate one deterministic context pack and launch one first-attempt execution path for a selected plan.</done>
</task>

<task type="auto">
  <name>Task 2: Expose manual plan-start, attempt detail, and evidence routes through the OMC API</name>
  <files>hopi/hub/src/web/routes/omc.ts, hopi/hub/src/web/routes/omc.test.ts, hopi/OMC-client/src/api/client.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/hub/src/web/routes/omc.ts, hopi/hub/src/web/routes/omc.test.ts, hopi/OMC-client/src/api/client.ts</read_first>
  <action>Extend the OMC route module with concrete endpoints for manual plan start, plan runtime detail, attempt detail, and evidence retrieval. Use stable route shapes aligned to the design work, including `POST /api/omc/programs/:programId/plans/:planKey/start`, `GET /api/omc/programs/:programId/plans/:planKey`, and `GET /api/omc/attempts/:attemptId`. Ensure responses include runtime state, attempt summaries, evidence metadata, and file/path references needed by the execution-first plan detail UI. Back these routes with focused tests.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the OMC start/detail routes pass tests and compile.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/web/routes/omc.ts` contains `plans/:planKey/start`
    - `hopi/hub/src/web/routes/omc.ts` contains `attempts/:attemptId`
    - `hopi/OMC-client/src/api/client.ts` contains `startPlan`
    - `hopi/OMC-client/src/api/client.ts` contains `getAttempt`
    - `hopi/hub/src/web/routes/omc.test.ts` contains `start`
  </acceptance_criteria>
  <done>OMC exposes real start/detail/evidence APIs instead of a read-only board-only surface.</done>
</task>

<task type="auto">
  <name>Task 3: Make plan detail execution-first and add the first attempt inspector UI</name>
  <files>hopi/OMC-client/src/routes/programs/plan.tsx, hopi/OMC-client/src/routes/programs/attempt.tsx, hopi/OMC-client/src/components/AttemptInspector.tsx, hopi/OMC-client/src/router.tsx</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/OMC-client/src/router.tsx, hopi/OMC-client/src/routes/programs/index.tsx, hopi/OMC-client/src/components/PlanBoard.tsx, hopi/hub/src/web/routes/omc.ts</read_first>
  <action>Implement the execution-first plan detail screen and the first attempt inspector UI in `OMC-client`. The plan detail route should place runtime summary, start action, attempts list, and evidence results above the parsed markdown/file-reference section. The attempt inspector should show the exact context pack, attempt status/timeline, changed-file summary, and evidence/check outputs without requiring the user to drop into legacy chat/session views. Include direct navigation between the plan detail and attempt inspector routes.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the plan detail and attempt inspector routes compile and build.</verify>
  <acceptance_criteria>
    - `hopi/OMC-client/src/routes/programs/plan.tsx` contains `startPlan`
    - `hopi/OMC-client/src/routes/programs/plan.tsx` contains `AttemptInspector`
    - `hopi/OMC-client/src/components/AttemptInspector.tsx` contains `context pack`
    - `hopi/OMC-client/src/components/AttemptInspector.tsx` contains `evidence`
    - `hopi/OMC-client/src/router.tsx` contains `/attempts/$attemptId`
  </acceptance_criteria>
  <done>OMC can manually start one plan and show the resulting attempt/evidence flow in execution-first UI surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run test:hub`
- [ ] `cd hopi && bun run typecheck:hub`
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] One plan can be started manually and viewed through execution-first plan/attempt surfaces
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC is no longer read-only: one manual plan start produces a visible attempt and evidence trail
- Plan detail and attempt inspection keep runtime state primary over markdown content
</success_criteria>

<output>
After completion, create .planning/phases/01-omc-foundation/01-03-SUMMARY.md
</output>
