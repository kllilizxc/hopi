---
phase: 02-loop-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - hopi/OMC-client/src/api/client.tsx
  - hopi/OMC-client/src/router.tsx
  - hopi/OMC-client/src/hooks/useOmcEvents.ts
  - hopi/OMC-client/src/components/PlanBoard.tsx
  - hopi/OMC-client/src/components/AttemptInspector.tsx
  - hopi/OMC-client/src/routes/programs/plan.tsx
  - hopi/OMC-client/src/routes/programs/attempt.tsx
  - hopi/hub/src/web/routes/omc.ts
autonomous: true
requirements:
  - OMC-02
  - OMC-05
  - OMC-06
must_haves:
    truths:
      - OMC-client exposes the new loop/review/takeover controls clearly without adding more board columns.
      - OMC-client reflects autonomous loop progress live; the user should not need manual refresh to see follow-up attempts or review escalation.
      - Review and takeover are visible as explicit control-plane states rather than hidden session rescue paths.
      - The user can deep-link into the existing HOPI session UI for takeover instead of needing a new embedded terminal.
      - Attempt and plan detail screens surface the new termination reason, retry memory, and evidence summaries produced by the backend.
    artifacts:
      - hopi/OMC-client/src/routes/programs/plan.tsx
      - hopi/OMC-client/src/components/AttemptInspector.tsx
      - hopi/OMC-client/src/components/PlanBoard.tsx
      - hopi/OMC-client/src/hooks/useOmcEvents.ts
      - hopi/OMC-client/src/api/client.tsx
    key_links:
      - The loop engine is only valuable if the OMC control plane makes retry/review/takeover states easy to see and act on.
      - Because autonomous retries happen without a local user mutation, OMC-client needs live invalidation/subscription rather than mutation-only refresh.
      - The user explicitly chose takeover via the existing HOPI session surface, so the UI should deep-link rather than embed a new session shell here.
      - Board columns stay minimal; richer loop semantics belong in badges, actions, and detail views.
---

<objective>
Surface the new loop-control and takeover model in OMC-client so the Phase 2 runtime becomes a usable control plane instead of a backend-only state machine.

Purpose: make the user-facing OMC surfaces reflect live loop state, review escalation, and human takeover/resume actions clearly.
Output: updated plan detail, attempt inspector, board badges, loop-control actions, and session deep-linking for takeover.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-loop-orchestration/02-CONTEXT.md
@.planning/phases/02-loop-orchestration/02-RESEARCH.md
@.planning/phases/02-loop-orchestration/02-VALIDATION.md
@.planning/phases/02-loop-orchestration/02-01-PLAN.md
@.planning/phases/02-loop-orchestration/02-02-PLAN.md
@hopi/AGENTS.md
@hopi/OMC-client/src/router.tsx
@hopi/OMC-client/src/routes/programs/plan.tsx
@hopi/OMC-client/src/routes/programs/attempt.tsx
@hopi/OMC-client/src/components/PlanBoard.tsx
@hopi/OMC-client/src/components/AttemptInspector.tsx
@hopi/web/src/hooks/useSSE.ts
@hopi/web/src/router.tsx
@hopi/web/src/routes/sessions/terminal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend plan detail and board surfaces with live loop state, review badges, and Phase 2 control actions</name>
  <files>hopi/OMC-client/src/routes/programs/plan.tsx, hopi/OMC-client/src/components/PlanBoard.tsx, hopi/OMC-client/src/api/client.tsx, hopi/OMC-client/src/hooks/useOmcEvents.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, .planning/phases/02-loop-orchestration/02-RESEARCH.md, hopi/OMC-client/src/routes/programs/plan.tsx, hopi/OMC-client/src/components/PlanBoard.tsx, hopi/web/src/hooks/useSSE.ts</read_first>
  <action>Update OMC-client plan and board surfaces so the new loop engine is readable and operable. Keep the four-column board, but add richer runtime badges and action affordances for `Take over`, `Resume loop`, `Open session`, and `Retry` in the plan detail. In the same slice, add OMC-side live event invalidation/subscription so autonomous retries and review escalations appear without manual refresh. Preserve the execution-first posture from Phase 1: runtime state and primary actions stay above parsed markdown content.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the updated plan/board surfaces compile and build successfully.</verify>
  <acceptance_criteria>
    - `hopi/OMC-client/src/routes/programs/plan.tsx` contains a takeover or resume action
    - `hopi/OMC-client/src/routes/programs/plan.tsx` contains an open-session or session link action
    - `hopi/OMC-client/src/components/PlanBoard.tsx` surfaces richer runtime metadata without adding new top-level columns
    - `hopi/OMC-client/src/hooks/useOmcEvents.ts` or equivalent exists for live OMC invalidation/subscription
    - `hopi/OMC-client/src/api/client.tsx` contains client helpers for the new loop-control endpoints
  </acceptance_criteria>
  <done>OMC plan and board surfaces make the Phase 2 loop/review/takeover model explicit, actionable, and live-updating.</done>
</task>

<task type="auto">
  <name>Task 2: Expand attempt inspection with termination reason, retry memory, and lightweight diff evidence</name>
  <files>hopi/OMC-client/src/components/AttemptInspector.tsx, hopi/OMC-client/src/routes/programs/attempt.tsx, hopi/hub/src/web/routes/omc.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, hopi/OMC-client/src/components/AttemptInspector.tsx, hopi/hub/src/web/routes/omc.ts</read_first>
  <action>Update the attempt inspector to show the new Phase 2 runtime information clearly: termination reason, retry memory carried into the next attempt, system-collected changed files, lightweight diff summary, and any review-escalation notes. Keep the surface readable and control-plane oriented rather than turning it into a raw log dump.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the attempt inspector changes compile and build successfully.</verify>
  <acceptance_criteria>
    - `hopi/OMC-client/src/components/AttemptInspector.tsx` contains termination-reason display
    - `hopi/OMC-client/src/components/AttemptInspector.tsx` contains retry-memory or previous-attempt summary display
    - `hopi/OMC-client/src/components/AttemptInspector.tsx` contains diff-summary or changed-file evidence display
  </acceptance_criteria>
  <done>Attempt detail reflects the full Phase 2 orchestration story, not just the original manual-start payload.</done>
</task>

<task type="auto">
  <name>Task 3: Deep-link takeover into the existing HOPI session experience and verify the end-to-end control flow</name>
  <files>hopi/OMC-client/src/routes/programs/plan.tsx, hopi/OMC-client/src/router.tsx, hopi/web/src/router.tsx</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, hopi/web/src/router.tsx, hopi/web/src/routes/sessions/terminal.tsx, hopi/OMC-client/src/router.tsx</read_first>
  <action>Implement the user-chosen takeover posture: OMC-client should deep-link into the existing HOPI session routes rather than hosting a new embedded session UI. Ensure the navigation feels intentional from the OMC control plane, and make sure the user can return to OMC to resume or review the loop afterward.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the takeover/session-link flows compile and build successfully.</verify>
  <acceptance_criteria>
    - `hopi/OMC-client/src/routes/programs/plan.tsx` contains a deep-link to the session experience
    - `hopi/OMC-client/src/router.tsx` still preserves the OMC route shell cleanly after adding the new action flows
    - takeover does not require a new embedded chat/terminal view inside OMC-client
  </acceptance_criteria>
  <done>OMC supports human takeover in the Phase 2-approved way: explicit control-plane action plus handoff into the existing session UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] Plan detail shows clear loop-control actions for takeover/resume/retry/open-session
- [ ] Attempt detail shows termination reason and lightweight diff/change evidence
- [ ] Takeover can deep-link into the existing HOPI session surface and back
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC-client exposes the Phase 2 orchestration engine as a real control plane
- Human takeover is explicit, legible, and aligned to the user's chosen session-deeplink posture
</success_criteria>

<output>
After completion, create .planning/phases/02-loop-orchestration/02-03-SUMMARY.md
</output>
