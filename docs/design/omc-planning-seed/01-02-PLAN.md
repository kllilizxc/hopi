---
phase: 01-omc-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - hopi/package.json
  - hopi/hub/src/web/server.ts
  - hopi/hub/scripts/generate-embedded-web-assets.ts
  - hopi/hub/src/web/embeddedAssets.ts
  - hopi/OMC-client/package.json
  - hopi/OMC-client/tsconfig.json
  - hopi/OMC-client/vite.config.ts
  - hopi/OMC-client/src/main.tsx
  - hopi/OMC-client/src/router.tsx
  - hopi/OMC-client/src/api/client.ts
  - hopi/OMC-client/src/routes/programs/index.tsx
  - hopi/OMC-client/src/routes/programs/plan.tsx
  - hopi/OMC-client/src/components/PlanBoard.tsx
autonomous: true
requirements:
  - OMC-01
  - OMC-02
  - OMC-03
user_setup: []
must_haves:
    truths:
      - `OMC-client/` exists as a real second frontend package, not just more routes inside `web/`.
      - The hub can serve OMC under `/omc` while preserving the existing session web app at `/`.
      - OMC board data comes from markdown-first planning index APIs, with phase grouping and `1 card = 1 PLAN.md`.
      - Board columns stay limited to `Planning`, `Running`, `Review`, and `Done`, with finer state expressed as card metadata.
    artifacts:
      - hopi/OMC-client/package.json
      - hopi/OMC-client/src/router.tsx
      - hopi/OMC-client/src/components/PlanBoard.tsx
      - hopi/hub/src/web/server.ts
      - hopi/hub/scripts/generate-embedded-web-assets.ts
    key_links:
      - Phase 1 explicitly requires a new frontend subproject, so build/serve plumbing is part of the product scope, not optional scaffolding.
      - The board is only valuable if it reads real `.planning` artifacts from the new OMC APIs.
      - `/omc` static serving and build integration must land before the first attempt UI can ship.
---

<objective>
Create the `OMC-client/` frontend package, wire the hub/build system to serve it under `/omc`, and render the first phase-grouped plan board from markdown-first planning APIs.

Purpose: make OMC a real second product shell with its own build, routing, and board UX.
Output: second frontend package, hub `/omc` static serving, and a phase-grouped board that renders `PLAN.md` cards from the planning index.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-omc-foundation/01-CONTEXT.md
@.planning/phases/01-omc-foundation/01-RESEARCH.md
@.planning/phases/01-omc-foundation/01-VALIDATION.md
@.planning/phases/01-omc-foundation/01-01-PLAN.md
@hopi/AGENTS.md
@hopi/package.json
@hopi/hub/src/web/server.ts
@hopi/hub/scripts/generate-embedded-web-assets.ts
@hopi/web/README.md
@hopi/web/src/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `OMC-client/` as a real workspace package with its own build and route shell</name>
  <files>hopi/package.json, hopi/OMC-client/package.json, hopi/OMC-client/tsconfig.json, hopi/OMC-client/vite.config.ts, hopi/OMC-client/src/main.tsx, hopi/OMC-client/src/router.tsx, hopi/OMC-client/src/api/client.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/package.json, hopi/web/package.json, hopi/web/vite.config.ts, hopi/web/src/main.tsx, hopi/web/src/router.tsx</read_first>
  <action>Create a new sibling package directory named `hopi/OMC-client/` with a Vite/React/TanStack Router setup aligned to the existing `web/` stack but not nested inside the current app. Update `hopi/package.json` workspaces and scripts to include concrete entries for `OMC-client`, `build:omc`, `dev:omc`, and `typecheck:omc`. Keep the package independent enough that OMC can evolve as its own product shell, while still reusing shared protocol types and hub auth/API conventions.</action>
  <verify>Run `cd hopi && bun run typecheck:omc` after the new package shell compiles cleanly.</verify>
  <acceptance_criteria>
    - `hopi/package.json` contains `"OMC-client"` in `workspaces`
    - `hopi/package.json` contains `"build:omc"`
    - `hopi/package.json` contains `"dev:omc"`
    - `hopi/OMC-client/package.json` contains `"name": "omc-client"`
    - `hopi/OMC-client/src/router.tsx` contains `/programs/$programId`
  </acceptance_criteria>
  <done>`OMC-client/` is a real package in the workspace and can typecheck independently.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the hub and embedded-asset pipeline to serve the second app under `/omc`</name>
  <files>hopi/hub/src/web/server.ts, hopi/hub/scripts/generate-embedded-web-assets.ts, hopi/hub/src/web/embeddedAssets.ts, hopi/package.json</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/hub/src/web/server.ts, hopi/hub/scripts/generate-embedded-web-assets.ts, hopi/hub/src/web/embeddedAssets.ts, hopi/package.json</read_first>
  <action>Extend the existing web-serving pipeline so the hub can serve the new `OMC-client` bundle under `/omc` without breaking the current session app at `/`. Add a concrete `findOmcDistDir` helper and `/omc` static-serving branch in `hopi/hub/src/web/server.ts`, update the embedded-asset generator to include OMC assets with a predictable namespace, and update root build scripts so `build` and `build:single-exe` account for the second frontend package. Do not collapse OMC into the existing `web/dist` catch-all path.</action>
  <verify>Run `cd hopi && bun run build:omc && bun run build:hub` after the second app serving/build path compiles cleanly.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/web/server.ts` contains `'/omc'`
    - `hopi/hub/src/web/server.ts` contains `findOmcDistDir`
    - `hopi/hub/scripts/generate-embedded-web-assets.ts` contains `OMC-client`
    - `hopi/package.json` contains `build:omc`
    - `hopi/package.json` contains `build:single-exe`
  </acceptance_criteria>
  <done>The hub and build pipeline can serve OMC as a real second app under `/omc`.</done>
</task>

<task type="auto">
  <name>Task 3: Build the first phase-grouped OMC board from markdown planning index data</name>
  <files>hopi/OMC-client/src/routes/programs/index.tsx, hopi/OMC-client/src/routes/programs/plan.tsx, hopi/OMC-client/src/components/PlanBoard.tsx, hopi/OMC-client/src/api/client.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/OMC-client/src/router.tsx, hopi/web/src/components/SessionList.tsx, hopi/web/src/hooks/queries/useSessions.ts, hopi/hub/src/web/routes/omc.ts</read_first>
  <action>Implement the first OMC board UI in `OMC-client` using the new OMC planning index APIs. Render cards grouped by phase and keep the top-level board columns fixed to `Planning`, `Running`, `Review`, and `Done`. Each card must represent exactly one `PLAN.md` and show concrete metadata drawn from the planning index/runtime contract: plan title, checklist counts, first open item, runtime state, and compact badges such as `ready-to-run` or latest failure summary. Include navigation from the board into the plan detail route, but keep the plan detail itself lightweight until Plan 01-03.</action>
  <verify>Run `cd hopi && bun run typecheck:omc` after the board routes/components compile against the OMC API client and shared types.</verify>
  <acceptance_criteria>
    - `hopi/OMC-client/src/components/PlanBoard.tsx` contains `Planning`
    - `hopi/OMC-client/src/components/PlanBoard.tsx` contains `Running`
    - `hopi/OMC-client/src/components/PlanBoard.tsx` contains `Review`
    - `hopi/OMC-client/src/components/PlanBoard.tsx` contains `Done`
    - `hopi/OMC-client/src/routes/programs/index.tsx` contains `planning-index`
  </acceptance_criteria>
  <done>OMC renders a real plan board from markdown-first planning data, with phase grouping and the locked Phase 1 column model.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] `cd hopi && bun run build:hub`
- [ ] `/omc` serving/build wiring exists without collapsing OMC into the legacy `web/` app
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `OMC-client/` exists as a separate package and is served under `/omc`
- The first OMC board renders real phase-grouped `PLAN.md` cards using markdown-first planning data
</success_criteria>

<output>
After completion, create .planning/phases/01-omc-foundation/01-02-SUMMARY.md
</output>
