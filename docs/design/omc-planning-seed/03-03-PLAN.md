---
phase: 03-review-and-merge-cockpit
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - OMC-client/src/api/client.tsx
  - OMC-client/src/router.tsx
  - OMC-client/src/hooks/useOmcEvents.ts
  - OMC-client/src/components/PlanBoard.tsx
  - OMC-client/src/components/ReviewCockpit.tsx
  - OMC-client/src/routes/programs/plan.tsx
  - OMC-client/src/routes/programs/review.tsx
  - OMC-client/src/index.css
autonomous: true
requirements:
  - OMC-05
  - OMC-06
must_haves:
    truths:
      - OMC has a dedicated review cockpit while keeping plan detail as the runtime home.
      - The cockpit is decision-first: blockers, checks, branches, attempts, and changed-files summary appear above deep evidence.
      - Review actions are explicit and distinct: `Approve review`, `Approve merge`, `Resume loop`, and `Back to planning`.
      - Merge-blocked and conflict states stay inside `Review` and lead to explicit actions like `Retry merge`, `Resolve conflicts`, and `Take over`.
      - Review and merge state stay live through dedicated OMC event invalidation.
    artifacts:
      - OMC-client/src/routes/programs/review.tsx
      - OMC-client/src/components/ReviewCockpit.tsx
      - OMC-client/src/routes/programs/plan.tsx
      - OMC-client/src/api/client.tsx
      - OMC-client/src/hooks/useOmcEvents.ts
    key_links:
      - The user explicitly chose a hybrid posture: runtime detail stays on the plan page while decisions move into a cockpit.
      - The merge packet must be decision-first, so the UI hierarchy matters as much as raw data correctness.
      - Conflict handling must remain in review posture and reuse takeover, not secretly jump states.
---

<objective>
Build the OMC review cockpit and decision-first review/merge UX on top of the new Phase 3 backend contracts.

Purpose: make review and merge decisions native, legible, and operator-friendly in OMC-client.
Output: review cockpit route, API client helpers, decision-first merge packet layout, live review/merge invalidation, and clear action hierarchy for approval, reopen, retry, takeover, and conflict resolution.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/03-CONTEXT.md
@docs/design/omc-planning-seed/03-RESEARCH.md
@docs/design/omc-planning-seed/03-VALIDATION.md
@docs/design/omc-planning-seed/03-01-PLAN.md
@docs/design/omc-planning-seed/03-02-PLAN.md
@AGENTS.md
@OMC-client/src/router.tsx
@OMC-client/src/api/client.tsx
@OMC-client/src/routes/programs/plan.tsx
@OMC-client/src/components/PlanBoard.tsx
@OMC-client/src/components/AttemptInspector.tsx
@OMC-client/src/hooks/useOmcEvents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated review cockpit route and client helpers for review/merge APIs</name>
  <files>OMC-client/src/router.tsx, OMC-client/src/api/client.tsx, OMC-client/src/routes/programs/plan.tsx, OMC-client/src/routes/programs/review.tsx</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, docs/design/omc-planning-seed/03-RESEARCH.md, OMC-client/src/router.tsx, OMC-client/src/api/client.tsx, OMC-client/src/routes/programs/plan.tsx</read_first>
  <action>Add a new review route at the concrete path `/programs/$programId/plans/$planKey/review` and a page module `OMC-client/src/routes/programs/review.tsx`. Extend `OmcApiClient` with concrete helpers for `getMergePacket`, `approveReview`, `reopenReview`, and `approveMerge`. Update `plan.tsx` so plans in review/merge state link intentionally into the review cockpit instead of trying to host all review decisions inline on the runtime page.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the new route and client helpers compile and build cleanly.</verify>
  <acceptance_criteria>
    - `OMC-client/src/router.tsx` contains `/programs/$programId/plans/$planKey/review`
    - `OMC-client/src/api/client.tsx` contains `getMergePacket`
    - `OMC-client/src/api/client.tsx` contains `approveReview`
    - `OMC-client/src/api/client.tsx` contains `reopenReview`
    - `OMC-client/src/api/client.tsx` contains `approveMerge`
    - `OMC-client/src/routes/programs/plan.tsx` contains a review cockpit link or navigation action
  </acceptance_criteria>
  <done>OMC-client has a dedicated review route and can call the new review/merge backend APIs directly.</done>
</task>

<task type="auto">
  <name>Task 2: Build a decision-first ReviewCockpit component with explicit review and merge actions</name>
  <files>OMC-client/src/components/ReviewCockpit.tsx, OMC-client/src/routes/programs/review.tsx, OMC-client/src/index.css</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, docs/design/factory-mode-ralph-gsd.md, OMC-client/src/components/AttemptInspector.tsx, OMC-client/src/index.css</read_first>
  <action>Create `OMC-client/src/components/ReviewCockpit.tsx` and use it from `review.tsx`. The first screenful must show the concrete decision-first sections in this order: merge verdict posture, blockers/warnings, required-check summary, source/target branch plus worktree identity, attempt count, and changed-files summary. Place deep evidence and attempt-history links lower on the page. Render explicit action buttons for `Approve review`, `Approve merge`, `Resume loop`, `Back to planning`, and `Take over`. When the packet/runtime shows `mergeStatus === "conflict"`, replace the primary merge CTA with `Resolve conflicts` and keep the page in review posture.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the cockpit component and styling compile and build successfully.</verify>
  <acceptance_criteria>
    - `OMC-client/src/components/ReviewCockpit.tsx` contains `Approve review`
    - `OMC-client/src/components/ReviewCockpit.tsx` contains `Approve merge`
    - `OMC-client/src/components/ReviewCockpit.tsx` contains `Resume loop`
    - `OMC-client/src/components/ReviewCockpit.tsx` contains `Back to planning`
    - `OMC-client/src/components/ReviewCockpit.tsx` contains `Resolve conflicts`
    - `OMC-client/src/components/ReviewCockpit.tsx` contains branch/worktree summary and changed-files summary sections
  </acceptance_criteria>
  <done>OMC exposes a real review cockpit that leads with merge decisions rather than logs or diff walls.</done>
</task>

<task type="auto">
  <name>Task 3: Keep review and merge state live across board, plan detail, and cockpit</name>
  <files>OMC-client/src/hooks/useOmcEvents.ts, OMC-client/src/components/PlanBoard.tsx, OMC-client/src/routes/programs/plan.tsx, OMC-client/src/routes/programs/review.tsx</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, OMC-client/src/hooks/useOmcEvents.ts, OMC-client/src/components/PlanBoard.tsx, OMC-client/src/routes/programs/plan.tsx</read_first>
  <action>Extend OMC live invalidation so review and merge state changes propagate without manual refresh. Handle the concrete event types `omc-review-updated` and `omc-merge-updated` in `useOmcEvents.ts`, invalidate the relevant program/board/plan/review queries, and surface merge-specific badges such as ready-to-merge, merge-blocked, and conflict without adding new top-level board columns. Keep the plan page as the runtime home while reflecting the new review/merge state clearly.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after review/merge live invalidation and badge changes compile and build cleanly.</verify>
  <acceptance_criteria>
    - `OMC-client/src/hooks/useOmcEvents.ts` contains `omc-review-updated`
    - `OMC-client/src/hooks/useOmcEvents.ts` contains `omc-merge-updated`
    - `OMC-client/src/components/PlanBoard.tsx` contains merge-specific badge rendering without adding new columns
    - `OMC-client/src/routes/programs/plan.tsx` reflects ready-to-merge or merge-blocked state
  </acceptance_criteria>
  <done>Review and merge state feel native and live across the OMC control plane rather than requiring refresh or hidden session knowledge.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] Review cockpit opens from a plan in `Review`
- [ ] Review cockpit top section answers merge readiness before deep evidence sections
- [ ] Conflict state shows `Resolve conflicts` without moving the card out of `Review`
- [ ] Review and merge state update live without manual reload
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC-client has a dedicated review cockpit with decision-first merge packet UX
- Review and merge actions are explicit, legible, and aligned with the user’s chosen operator posture
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/03-03-SUMMARY.md
</output>
