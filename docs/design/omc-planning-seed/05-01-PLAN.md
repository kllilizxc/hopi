---
phase: 05-guided-planning-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/sync/omc/events.ts
  - hub/src/sync/omc/runtimeStore.ts
  - hub/src/web/routes/omc.ts
  - hub/src/web/routes/omc.test.ts
autonomous: true
requirements:
  - OMC-11
  - OMC-12
  - OMC-13
must_haves:
    truths:
      - Guided planning after seed is a first-class OMC runtime, not an ad-hoc frontend-only flag.
      - Guided planning is modeled at the `Program` level because no `PLAN.md` cards exist yet.
      - OMC exposes typed route contracts and realtime events for reading planning-run state.
      - Focused tests protect the new planning-run persistence and route surfaces before orchestration/UI work starts.
    artifacts:
      - shared/src/schemas.ts
      - shared/src/types.ts
      - hub/src/store/index.ts
      - hub/src/store/types.ts
      - hub/src/sync/omc/events.ts
      - hub/src/sync/omc/runtimeStore.ts
      - hub/src/web/routes/omc.ts
      - hub/src/web/routes/omc.test.ts
    key_links:
      - Phase 5 cannot safely reuse plan-attempt runtime because the gap happens before any real plan card exists.
      - Product-visible guided planning requires canonical state, not just imperative mutations.
---

<objective>
Add the program-level guided-planning contracts and persistence layer for OMC.

Purpose: establish a canonical runtime model for “seed scaffold exists, planning continuation is now running.”
Output: shared schemas/types, runtime store support, realtime events, read APIs, and regression tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/05-CONTEXT.md
@docs/design/omc-planning-seed/05-RESEARCH.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/STATE.md
@AGENTS.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/store/index.ts
@hub/src/store/types.ts
@hub/src/sync/omc/events.ts
@hub/src/sync/omc/runtimeStore.ts
@hub/src/web/routes/omc.ts
@hub/src/web/routes/omc.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared guided-planning runtime contracts</name>
  <files>shared/src/schemas.ts, shared/src/types.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-CONTEXT.md, docs/design/omc-planning-seed/05-RESEARCH.md, shared/src/schemas.ts</read_first>
  <action>Add explicit shared schemas/types for a program-level guided-planning run, its brief payload, its lifecycle status, and the route contracts the frontend will consume. Keep the model program-scoped rather than plan-scoped.</action>
  <verify>Run `bun run typecheck` after shared contract additions compile across packages.</verify>
  <acceptance_criteria>
    - guided-planning run schemas exist in `shared/src/schemas.ts`
    - guided-planning run types are exported from `shared/src/types.ts`
    - route payloads distinguish brief input, run status, and resulting planning state
  </acceptance_criteria>
  <done>Phase 5 has a typed contract for the new program-level planning runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Persist guided-planning runtime state and publish realtime events</name>
  <files>hub/src/store/index.ts, hub/src/store/types.ts, hub/src/sync/omc/events.ts, hub/src/sync/omc/runtimeStore.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-RESEARCH.md, hub/src/store/index.ts, hub/src/sync/omc/runtimeStore.ts, hub/src/sync/omc/events.ts</read_first>
  <action>Extend the hub store/runtime layer with program-level planning-run persistence and event builders so the product can observe guided-planning progress, failure, cancellation, and completion just like other OMC runtime state.</action>
  <verify>Run `bun run typecheck:hub` after the new store/runtime/event surfaces compile cleanly.</verify>
  <acceptance_criteria>
    - store schema supports program-level guided-planning runtime rows
    - runtime store can read and write guided-planning runs
    - OMC events expose planning-run updates for SSE invalidation
  </acceptance_criteria>
  <done>Guided planning is stored durably and can be broadcast to OMC clients.</done>
</task>

<task type="auto">
  <name>Task 3: Expose read-side guided-planning APIs and tests</name>
  <files>hub/src/web/routes/omc.ts, hub/src/web/routes/omc.test.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-CONTEXT.md, docs/design/omc-planning-seed/05-RESEARCH.md, hub/src/web/routes/omc.ts, hub/src/web/routes/omc.test.ts</read_first>
  <action>Add read-side OMC APIs for retrieving the current program-level guided-planning state and cover them with focused route tests. Do not implement the orchestration start/cancel/retry behavior yet; this plan only establishes the canonical state surface.</action>
  <verify>Run `bun test hub/src/web/routes/omc.test.ts` after the new read-side APIs are added.</verify>
  <acceptance_criteria>
    - `hub/src/web/routes/omc.ts` exposes a guided-planning state read route
    - `hub/src/web/routes/omc.test.ts` covers the new read route and stored state shape
  </acceptance_criteria>
  <done>Frontend work can now read a real guided-planning runtime instead of inferring from missing plan cards.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck`
- [ ] `bun run typecheck:hub`
- [ ] `bun test hub/src/web/routes/omc.test.ts`
- [ ] Guided planning has a durable program-level runtime model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC has a canonical program-level guided-planning runtime contract before orchestration begins
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/05-01-SUMMARY.md
</output>
