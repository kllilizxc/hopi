---
phase: 04-program-bootstrap-and-planning-attach
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - OMC-client/src/api/client.tsx
  - OMC-client/src/router.tsx
  - OMC-client/src/routes/programs/index.tsx
  - OMC-client/src/components/ProgramAttachPanel.tsx
  - OMC-client/src/index.css
autonomous: true
requirements:
  - OMC-09
must_haves:
    truths:
      - OMC-client lets a user attach a local repo from the UI instead of requiring env-var setup.
      - The Programs surface can handle more than one attached repo without blindly hiding newly attached programs.
      - Attach UX is lightweight and practical: path input, validation feedback, and direct navigation into the new program.
      - The frontend uses real backend attach/bootstrap contracts rather than hard-coded repo assumptions.
    artifacts:
      - OMC-client/src/api/client.tsx
      - OMC-client/src/routes/programs/index.tsx
      - OMC-client/src/components/ProgramAttachPanel.tsx
      - OMC-client/src/index.css
    key_links:
      - The user explicitly wants flows like `/Users/realizer/Code/PersonalQuant`, so attach must start from a concrete local path.
      - Phase 4 is meant to remove the current dead-end onboarding posture, and Programs is the natural entry point.
      - Multi-program visibility becomes important immediately once external attach exists.
---

<objective>
Build the OMC attach-program UX for local repos.

Purpose: let a user attach a repo such as `PersonalQuant` from OMC-client without touching env vars or backend state manually.
Output: attach repo client helpers, a program attach panel, and sane program-list behavior after attach.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/04-CONTEXT.md
@docs/design/omc-planning-seed/04-RESEARCH.md
@docs/design/omc-planning-seed/04-VALIDATION.md
@docs/design/omc-planning-seed/04-01-PLAN.md
@AGENTS.md
@OMC-client/src/api/client.tsx
@OMC-client/src/router.tsx
@OMC-client/src/routes/programs/index.tsx
@OMC-client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OMC-client helpers for repo attach and planning bootstrap APIs</name>
  <files>OMC-client/src/api/client.tsx</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/omc-planning-seed/04-RESEARCH.md, OMC-client/src/api/client.tsx</read_first>
  <action>Extend `OmcApiClient` with concrete client helpers for Phase 4 attach/bootstrap endpoints. Keep the types explicit enough that the frontend can distinguish: attached program returned, planning detected vs missing, explicit planning attach success, and seed creation success. Preserve the current auth/token flow while adding the new OMC methods.</action>
  <verify>Run `cd hopi && bun run typecheck:omc` after the client helper additions compile cleanly.</verify>
  <acceptance_criteria>
    - `OMC-client/src/api/client.tsx` contains local repo attach helper(s)
    - `OMC-client/src/api/client.tsx` contains planning attach helper(s)
    - `OMC-client/src/api/client.tsx` contains planning seed helper(s)
  </acceptance_criteria>
  <done>OMC-client can call the full Phase 4 backend contract directly.</done>
</task>

<task type="auto">
  <name>Task 2: Build a lightweight Programs attach surface for absolute local repo paths</name>
  <files>OMC-client/src/routes/programs/index.tsx, OMC-client/src/components/ProgramAttachPanel.tsx, OMC-client/src/index.css</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, OMC-client/src/routes/programs/index.tsx, OMC-client/src/index.css</read_first>
  <action>Create a lightweight attach surface on the Programs page that accepts an absolute local repo path such as `/Users/realizer/Code/PersonalQuant`, calls the backend attach API, shows validation errors clearly, and navigates into the newly attached program on success. The UI should feel intentionally practical, not wizard-heavy.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the attach surface compiles and builds successfully.</verify>
  <acceptance_criteria>
    - `OMC-client/src/routes/programs/index.tsx` contains a visible attach-program entry point
    - `OMC-client/src/components/ProgramAttachPanel.tsx` exists
    - `OMC-client/src/components/ProgramAttachPanel.tsx` contains the `PersonalQuant`-style absolute-path posture
  </acceptance_criteria>
  <done>Users can attach a local repo from OMC-client without leaving the product.</done>
</task>

<task type="auto">
  <name>Task 3: Make program selection behavior safe once more than one program exists</name>
  <files>OMC-client/src/routes/programs/index.tsx, OMC-client/src/router.tsx</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/omc-planning-seed/04-RESEARCH.md, OMC-client/src/routes/programs/index.tsx, OMC-client/src/router.tsx</read_first>
  <action>Adjust current Programs behavior so attaching a new repo does not get obscured by unconditional auto-navigation to the first program. Preserve a fast path when there is exactly one program, but once multiple programs exist the newly attached program must stay visible and reachable.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after attach navigation logic compiles and builds cleanly.</verify>
  <acceptance_criteria>
    - `OMC-client/src/routes/programs/index.tsx` no longer blindly hides multi-program state
    - the newly attached program can be navigated to directly after attach
  </acceptance_criteria>
  <done>Program attach behaves predictably even when OMC has more than one repo.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] Programs page can attach a local repo by absolute path
- [ ] Newly attached program remains visible when multiple programs exist
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC-client has a usable attach-program entry point for local repos
- Program selection behavior remains clear after attach
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/04-02-SUMMARY.md
</output>
