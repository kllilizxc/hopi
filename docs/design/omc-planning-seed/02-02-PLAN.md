---
phase: 02-loop-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - hopi/hub/src/sync/omc/loopController.ts
  - hopi/hub/src/sync/omc/attemptRunner.ts
  - hopi/hub/src/sync/omc/contextPackBuilder.ts
  - hopi/hub/src/sync/omc/runtimeStore.ts
  - hopi/hub/src/sync/omc/evidenceCollector.ts
  - hopi/hub/src/sync/omc/events.ts
  - hopi/hub/src/web/routes/omc.ts
  - hopi/hub/src/web/routes/omc.test.ts
  - hopi/hub/src/sync/syncEngine.ts
autonomous: true
requirements:
  - OMC-02
  - OMC-03
  - OMC-04
  - OMC-05
  - OMC-06
must_haves:
    truths:
      - OMC turns normalized attempt outcomes into real Ralph loop transitions inside one plan.
      - Follow-up attempts continue to rebuild context from markdown planning files rather than introducing a new structured planning source of truth.
      - `progressed`, `blocked`, `failed`, `completed`, and `canceled` each lead to the locked Phase 2 transition behavior.
      - Evidence for review and retry memory includes system-collected changed-file and lightweight diff information, not only agent self-report.
      - Loop control actions exist server-side for retry, resume, takeover/review, and cancellation state changes.
    artifacts:
      - hopi/hub/src/sync/omc/loopController.ts
      - hopi/hub/src/sync/omc/contextPackBuilder.ts
      - hopi/hub/src/sync/omc/evidenceCollector.ts
      - hopi/hub/src/web/routes/omc.ts
      - hopi/hub/src/web/routes/omc.test.ts
    key_links:
      - The loop-policy engine is the heart of the Ralph behavior; without it, OMC is still a one-attempt launcher.
      - The user explicitly kept planning truth in markdown, so each fresh attempt must continue to derive its planning context from `.planning/*`.
      - The user explicitly wants system-collected diff/change evidence and fast escalation for blocked work.
      - Review/takeover actions should exist as backend state transitions before the OMC UI tries to present them.
---

<objective>
Build the OMC loop-policy engine and evidence harvesting layer so one plan can automatically continue, escalate, or stop based on normalized attempt outcomes.

Purpose: turn attempt outcomes into real loop behavior and make review/debug evidence reliable enough for human gating.
Output: loop controller, policy-driven follow-up attempts, system-collected evidence, and OMC loop-control API endpoints.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-loop-orchestration/02-CONTEXT.md
@.planning/phases/02-loop-orchestration/02-RESEARCH.md
@.planning/phases/02-loop-orchestration/02-VALIDATION.md
@.planning/phases/02-loop-orchestration/02-01-PLAN.md
@hopi/AGENTS.md
@hopi/hub/src/sync/omc/attemptRunner.ts
@hopi/hub/src/sync/omc/contextPackBuilder.ts
@hopi/hub/src/sync/omc/evidenceCollector.ts
@hopi/hub/src/web/routes/omc.ts
@hopi/hub/src/web/routes/git.ts
@hopi/hub/src/sync/syncEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Phase 2 loop-policy controller for automatic continue vs review escalation</name>
  <files>hopi/hub/src/sync/omc/loopController.ts, hopi/hub/src/sync/omc/attemptRunner.ts, hopi/hub/src/sync/omc/contextPackBuilder.ts, hopi/hub/src/sync/omc/runtimeStore.ts, hopi/hub/src/sync/omc/events.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, .planning/phases/02-loop-orchestration/02-RESEARCH.md, hopi/hub/src/sync/omc/attemptRunner.ts, hopi/hub/src/sync/omc/contextPackBuilder.ts, hopi/hub/src/sync/omc/runtimeStore.ts</read_first>
  <action>Create a loop controller that consumes the normalized attempt outcome contract and applies the locked Phase 2 policy: `progressed` can auto-continue inside the same plan, `blocked` goes directly to `Review`, `failed` retries until the failure/attempt caps are hit, `completed` always enters `Review`, and `canceled` stops autonomous execution without auto-retrying. Keep loop continuity inside the same `loopRun`, reuse the plan worktree as the loop advances, and rebuild each new attempt context from the current markdown planning files plus the minimal retry-memory contract.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the loop-policy controller and runtime transition logic are covered by OMC tests.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/sync/omc/loopController.ts` contains `applyOmcLoopPolicy`
    - `hopi/hub/src/sync/omc/loopController.ts` contains explicit handling for `progressed`
    - `hopi/hub/src/sync/omc/loopController.ts` contains explicit handling for `blocked`
    - `hopi/hub/src/sync/omc/loopController.ts` contains explicit handling for `failed`
    - `hopi/hub/src/sync/omc/contextPackBuilder.ts` refreshes next-attempt planning context from markdown refs
    - `hopi/hub/src/sync/omc/runtimeStore.ts` records the resulting runtime/review state transitions
  </acceptance_criteria>
  <done>OMC behaves like a real within-plan Ralph loop rather than a one-shot attempt launcher.</done>
</task>

<task type="auto">
  <name>Task 2: Collect system-grounded attempt evidence for changed files, diff summary, and termination reason</name>
  <files>hopi/hub/src/sync/omc/evidenceCollector.ts, hopi/hub/src/sync/syncEngine.ts, hopi/hub/src/web/routes/omc.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, hopi/hub/src/sync/omc/evidenceCollector.ts, hopi/hub/src/web/routes/tasks.ts, hopi/hub/src/sync/syncEngine.ts</read_first>
  <action>Extend OMC evidence collection so review and retry memory do not rely only on agent self-report. Reuse existing session/worktree git primitives to gather changed files and a lightweight diff summary, and record an explicit termination reason alongside the structured completion summary and checks. Keep the evidence light enough for plan detail and next-attempt memory; do not store full diff blobs or full logs in the context pack.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after evidence harvesting is covered by OMC tests and compiles cleanly.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/sync/omc/evidenceCollector.ts` contains system-collected changed-file or diff-summary logic
    - `hopi/hub/src/sync/omc/evidenceCollector.ts` contains `termination` or `termination reason`
    - `hopi/hub/src/web/routes/omc.ts` returns the richer evidence payload in plan/attempt detail responses
  </acceptance_criteria>
  <done>Attempt evidence is grounded in real worktree state and explicit runtime outcomes, not only in agent summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend loop-control endpoints for retry, resume, takeover, and cancellation state changes</name>
  <files>hopi/hub/src/web/routes/omc.ts, hopi/hub/src/web/routes/omc.test.ts, hopi/hub/src/sync/omc/loopController.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/02-loop-orchestration/02-CONTEXT.md, hopi/hub/src/web/routes/omc.ts, hopi/hub/src/web/routes/omc.test.ts</read_first>
  <action>Expose the Phase 2 control-plane actions that the UI will need: retry / resume loop, takeover preparation, open-review state, and explicit cancellation. These endpoints should be thin wrappers over the new loop-policy/orchestration layer rather than ad hoc mutations. Ensure the returned payloads include the updated plan runtime, any new attempt record, and session linkage needed for takeover/open-session flows.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the loop-control routes pass focused OMC tests.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/web/routes/omc.ts` contains `plans/:planKey/retry`
    - `hopi/hub/src/web/routes/omc.ts` contains `plans/:planKey/resume`
    - `hopi/hub/src/web/routes/omc.ts` contains `plans/:planKey/takeover`
    - `hopi/hub/src/web/routes/omc.test.ts` covers at least one non-start loop control action
  </acceptance_criteria>
  <done>OMC backend exposes real loop/review/takeover control actions instead of only the initial manual start.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run test:hub`
- [ ] `cd hopi && bun run typecheck:hub`
- [ ] One `progressed` attempt can auto-trigger the next attempt inside the same loop run
- [ ] One `blocked` or budget-exhausted attempt can move the plan into `Review`
- [ ] Attempt detail exposes system-collected changed-file / diff-summary evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC can automatically continue or escalate a plan based on normalized attempt outcomes
- Review and retry decisions are evidence-backed rather than only agent-declared
</success_criteria>

<output>
After completion, create .planning/phases/02-loop-orchestration/02-02-SUMMARY.md
</output>
