---
phase: 04-program-bootstrap-and-planning-attach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/sync/omc/programPaths.ts
  - hub/src/sync/omc/programBootstrap.ts
  - hub/src/web/routes/omc.ts
  - hub/src/web/routes/omc.test.ts
autonomous: true
requirements:
  - OMC-09
  - OMC-10
must_haves:
    truths:
      - OMC can attach an explicit local git repo as a `Program` instead of only relying on the default hub repo.
      - OMC has explicit backend contracts for attach local repo, attach planning root, and create planning seed.
      - Planning seed generation writes a minimal markdown scaffold inside the attached repo at `.planning/*`.
      - Invalid attach inputs are rejected clearly before any program record or planning seed is created.
      - OMC route tests cover attach/bootstrap behavior with temp repos rather than relying on real user repos.
    artifacts:
      - shared/src/schemas.ts
      - shared/src/types.ts
      - hub/src/sync/omc/programBootstrap.ts
      - hub/src/web/routes/omc.ts
      - hub/src/web/routes/omc.test.ts
    key_links:
      - Phase 4 cannot fix the `PersonalQuant` journey until repoRoot rebinding and planning seed creation are real backend capabilities.
      - The user explicitly chose local git repos only and minimal repo-local planning seeds, so the backend must enforce those boundaries.
      - Route-level validation belongs before any frontend attach wizard or empty-state polish.
---

<objective>
Add the backend contract for attaching local repos and bootstrapping missing planning in OMC.

Purpose: remove the hard-coded single-repo assumption and give the frontend real attach/bootstrap APIs.
Output: shared request/response schemas, hub attach/bootstrap routes, repo validation, seed writer, and focused tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/04-CONTEXT.md
@docs/design/omc-planning-seed/04-RESEARCH.md
@docs/design/omc-planning-seed/04-VALIDATION.md
@AGENTS.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/sync/omc/runtimeStore.ts
@hub/src/sync/omc/programPaths.ts
@hub/src/web/routes/omc.ts
@hub/src/web/routes/omc.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit OMC attach/bootstrap request contracts and backend helpers</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, hub/src/sync/omc/programPaths.ts, hub/src/sync/omc/programBootstrap.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/omc-planning-seed/04-RESEARCH.md, shared/src/schemas.ts, hub/src/sync/omc/programPaths.ts</read_first>
  <action>Extend shared OMC schemas/types with concrete request/response payloads for attaching a local repo, attaching an explicit planning root, and creating a planning seed. Add a new hub utility module `hub/src/sync/omc/programBootstrap.ts` that validates absolute local git repo paths, resolves/detects planning roots, rejects duplicate attached repos for the same namespace, and can write the minimal repo-local `.planning/*` scaffold selected in Phase 4 context.</action>
  <verify>Run `cd hopi && bun run typecheck` after the new shared/backend attach-bootstrap contracts compile across packages.</verify>
  <acceptance_criteria>
    - `shared/src/schemas.ts` contains attach/bootstrap OMC request schemas
    - `hub/src/sync/omc/programBootstrap.ts` validates local git repos
    - `hub/src/sync/omc/programBootstrap.ts` writes `.planning/PROJECT.md`
    - `hub/src/sync/omc/programBootstrap.ts` writes `.planning/phases/01-bootstrap/01-CONTEXT.md`
  </acceptance_criteria>
  <done>Phase 4 has explicit, typed backend building blocks for repo attach and planning bootstrap.</done>
</task>

<task type="auto">
  <name>Task 2: Expose OMC routes for attach local repo, attach planning root, and create planning seed</name>
  <files>hub/src/web/routes/omc.ts, hub/src/sync/omc/programBootstrap.ts, hub/src/sync/omc/runtimeStore.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/omc-planning-seed/04-RESEARCH.md, hub/src/web/routes/omc.ts, hub/src/sync/omc/runtimeStore.ts</read_first>
  <action>Add OMC-native routes for: attaching a local repo as a program, attaching an explicit planning root for that program, and creating the minimal planning seed when none exists. Ensure the attach flow persists a real `Program` with the attached `repoRoot`, not the hub repo. Keep the default program fallback for backward compatibility inside this repo, but do not let it override newly attached programs. Route responses should give the frontend enough information to navigate into the attached program and reflect whether planning was detected, attached, or newly seeded.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts && bun run typecheck:hub` after the new routes compile and test cleanly.</verify>
  <acceptance_criteria>
    - `hub/src/web/routes/omc.ts` contains a route for attaching a local repo
    - `hub/src/web/routes/omc.ts` contains a route for attaching an explicit planning root
    - `hub/src/web/routes/omc.ts` contains a route for creating a planning seed
    - attached programs persist the requested repo root instead of the hub repo root
  </acceptance_criteria>
  <done>OMC has backend APIs for the full Phase 4 attach/bootstrap flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused attach/bootstrap tests using temp repos and planning fixtures</name>
  <files>hub/src/web/routes/omc.test.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-VALIDATION.md, hub/src/web/routes/omc.test.ts</read_first>
  <action>Extend `hub/src/web/routes/omc.test.ts` with targeted cases for attaching a valid local git repo, rejecting invalid attach inputs, attaching an explicit planning root, and creating a minimal planning seed into a repo that started without `.planning`. Keep the tests OMC-focused and use temp directories plus lightweight git initialization where needed.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts` after the new attach/bootstrap tests are added.</verify>
  <acceptance_criteria>
    - `hub/src/web/routes/omc.test.ts` contains attach-local-repo coverage
    - `hub/src/web/routes/omc.test.ts` contains attach-planning-root coverage
    - `hub/src/web/routes/omc.test.ts` contains planning-seed coverage
  </acceptance_criteria>
  <done>Phase 4 has a regression harness for repo attach and planning bootstrap before UI work starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck`
- [ ] `cd hopi && bun test hub/src/web/routes/omc.test.ts`
- [ ] OMC can persist a new program whose `repoRoot` is not the hub repo
- [ ] OMC can create the minimal repo-local `.planning/*` seed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC can attach a local repo and bootstrap missing planning through explicit backend APIs
- Repo-local planning seed creation matches the locked Phase 4 scope
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/04-01-SUMMARY.md
</output>
