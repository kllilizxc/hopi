---
phase: 01-omc-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hopi/shared/src/schemas.ts
  - hopi/shared/src/types.ts
  - hopi/shared/src/index.ts
  - hopi/hub/src/store/index.ts
  - hopi/hub/src/store/types.ts
  - hopi/hub/src/web/server.ts
  - hopi/hub/src/web/routes/omc.ts
  - hopi/hub/src/web/routes/omc.test.ts
  - hopi/hub/src/sync/omc/planningIndex.ts
  - hopi/hub/src/sync/omc/runtimeStore.ts
  - hopi/hub/src/sync/omc/events.ts
autonomous: true
requirements:
  - OMC-01
  - OMC-04
  - OMC-05
  - OMC-08
user_setup: []
must_haves:
    truths:
      - HOPI gains a native OMC runtime contract in shared and hub layers instead of reusing task status as the canonical orchestration state.
      - Phase 1 runtime persistence can represent one program, per-plan runtime state, attempts, and evidence with durable API/SSE payloads.
      - The hub exposes a dedicated `/api/omc/*` namespace so OMC does not piggyback on legacy task routes.
      - The initial OMC domain remains single-repo and plan-oriented, matching the locked Phase 1 scope.
    artifacts:
      - hopi/shared/src/schemas.ts
      - hopi/shared/src/types.ts
      - hopi/hub/src/web/routes/omc.ts
      - hopi/hub/src/sync/omc/planningIndex.ts
      - hopi/hub/src/sync/omc/runtimeStore.ts
      - hopi/hub/src/web/server.ts
    key_links:
      - Phase 1 needs runtime contracts before the new board can consume real data.
      - The new OMC route namespace and event model must exist before `OMC-client/` can be built safely.
      - Single-repo scope and first-attempt visibility are locked requirements, so the initial contracts should not pretend multi-repo or full scheduler support already exists.
---

<objective>
Seed the OMC domain model and hub-side runtime/API foundation so later plans can render real OMC data and trigger a real first attempt.

Purpose: create the typed backend contract for programs, plan runtimes, attempts, evidence, and OMC-specific events.
Output: shared schemas/types, hub persistence helpers, planning/runtime route scaffolding, and a dedicated `/api/omc/*` entry path.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-omc-foundation/01-CONTEXT.md
@.planning/phases/01-omc-foundation/01-RESEARCH.md
@.planning/phases/01-omc-foundation/01-VALIDATION.md
@hopi/AGENTS.md
@hopi/shared/src/schemas.ts
@hopi/shared/src/types.ts
@hopi/shared/src/index.ts
@hopi/hub/src/store/index.ts
@hopi/hub/src/store/types.ts
@hopi/hub/src/web/server.ts
@hopi/hub/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce OMC shared contracts for program, plan runtime, attempt, evidence, and board/runtime payloads</name>
  <files>hopi/shared/src/schemas.ts, hopi/shared/src/types.ts, hopi/shared/src/index.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/shared/src/schemas.ts, hopi/shared/src/types.ts, hopi/shared/src/index.ts</read_first>
  <action>Add explicit OMC schemas and exported types in `hopi/shared/src/` for the locked Phase 1 runtime model. Create concrete identifiers named `OmcProgramSchema`, `OmcPlanRuntimeSchema`, `OmcAttemptSchema`, `OmcEvidenceSchema`, and status enums such as `OmcBoardColumnSchema`, `OmcLoopStatusSchema`, and `OmcAttemptStatusSchema`. Include fields that match the Phase 1 design: single primary repo, `planKey`, `planPath`, board column, worktree path, target branch, attempt counts, failure fingerprint, evidence summary, and human-review posture. Export the new schemas/types from `hopi/shared/src/index.ts` so hub and `OMC-client` can consume one shared contract.</action>
  <verify>Run `cd hopi && bun run typecheck:hub` after the shared OMC schema/type exports compile cleanly through the hub package.</verify>
  <acceptance_criteria>
    - `hopi/shared/src/schemas.ts` contains `export const OmcProgramSchema`
    - `hopi/shared/src/schemas.ts` contains `export const OmcPlanRuntimeSchema`
    - `hopi/shared/src/schemas.ts` contains `export const OmcAttemptSchema`
    - `hopi/shared/src/schemas.ts` contains `export const OmcEvidenceSchema`
    - `hopi/shared/src/index.ts` contains `OmcProgramSchema`
  </acceptance_criteria>
  <done>Shared code exposes a stable OMC runtime contract that later hub/frontend work can import directly.</done>
</task>

<task type="auto">
  <name>Task 2: Add hub-side OMC persistence helpers and runtime read/write seams</name>
  <files>hopi/hub/src/store/index.ts, hopi/hub/src/store/types.ts, hopi/hub/src/sync/omc/runtimeStore.ts, hopi/hub/src/sync/omc/events.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/hub/src/store/index.ts, hopi/hub/src/store/types.ts, hopi/shared/src/schemas.ts</read_first>
  <action>Create the first OMC hub persistence layer using the existing SQLite/store architecture. Introduce a concrete `export class OmcRuntimeStore` in `hopi/hub/src/sync/omc/runtimeStore.ts` plus any needed store/type wiring so the hub can persist one program, plan runtime rows, attempts, and evidence summaries without overloading legacy task tables as canonical truth. Add a concrete `export type OmcPlanRuntimeRow` in `hopi/hub/src/store/types.ts`. Add OMC-specific realtime event helpers in `hopi/hub/src/sync/omc/events.ts` with event ids `omc-program-updated`, `omc-plan-runtime-updated`, `omc-attempt-added`, and `omc-evidence-added`.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the new OMC runtime helpers and store wiring compile and any new route/store tests pass.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/sync/omc/runtimeStore.ts` contains `export class OmcRuntimeStore`
    - `hopi/hub/src/sync/omc/events.ts` contains `omc-plan-runtime-updated`
    - `hopi/hub/src/sync/omc/events.ts` contains `omc-attempt-added`
    - `hopi/hub/src/store/types.ts` contains `export type OmcPlanRuntimeRow`
  </acceptance_criteria>
  <done>Hub code has a durable OMC runtime seam with explicit persistence and event vocabulary.</done>
</task>

<task type="auto">
  <name>Task 3: Add the first `/api/omc/*` routes and planning/runtime response scaffolding</name>
  <files>hopi/hub/src/web/routes/omc.ts, hopi/hub/src/web/server.ts, hopi/hub/src/sync/omc/planningIndex.ts, hopi/hub/src/web/routes/omc.test.ts</files>
  <read_first>hopi/AGENTS.md, .planning/phases/01-omc-foundation/01-CONTEXT.md, .planning/phases/01-omc-foundation/01-RESEARCH.md, hopi/hub/src/web/server.ts, hopi/hub/src/web/routes/projects.ts, hopi/hub/src/web/routes/tasks.ts, hopi/hub/README.md</read_first>
  <action>Create a dedicated `hopi/hub/src/web/routes/omc.ts` module and register it from `hopi/hub/src/web/server.ts`. Implement the first OMC route surface with concrete read endpoints for program listing, one program overview, and placeholder/runtime-backed plan responses that later plans will consume. Also add the first markdown planning indexer seam in `hopi/hub/src/sync/omc/planningIndex.ts` with a concrete exported API for scanning `.planning/phases/**/*PLAN.md`, deriving `planKey`, `phaseKey`, checklist counts, and summary metadata. Back the new route module with focused hub tests in `hopi/hub/src/web/routes/omc.test.ts`.</action>
  <verify>Run `cd hopi && bun run test:hub && bun run typecheck:hub` after the OMC route module is registered and the new planning/runtime endpoints pass tests.</verify>
  <acceptance_criteria>
    - `hopi/hub/src/web/routes/omc.ts` contains `createOmcRoutes`
    - `hopi/hub/src/web/server.ts` contains `createOmcRoutes`
    - `hopi/hub/src/web/server.ts` contains `app.route('/api', createOmcRoutes`
    - `hopi/hub/src/sync/omc/planningIndex.ts` contains `planning-index`
    - `hopi/hub/src/web/routes/omc.test.ts` contains `planning-index`
  </acceptance_criteria>
  <done>The hub exposes a first-class OMC API namespace and a real planning-index seam for the next plan to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run test:hub`
- [ ] `cd hopi && bun run typecheck:hub`
- [ ] OMC shared/runtime route contracts exist without using `TaskStatus` as the canonical OMC state machine
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC backend/runtime contracts exist as native shared+hub code, not as comments or mock types
- `/api/omc/*` exists as a real route namespace with typed planning/runtime response scaffolding
</success_criteria>

<output>
After completion, create .planning/phases/01-omc-foundation/01-01-SUMMARY.md
</output>
