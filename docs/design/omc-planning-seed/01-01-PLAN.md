---
phase: 01-omc-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts
  - /Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx
  - /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts
  - /Users/realizer/Code/hopi/omc-prototype/src/prototype/scenario.ts
  - /Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts
autonomous: true
requirements:
  - OMC-14
  - OMC-15
  - OMC-16
  - OMC-17
user_setup: []
must_haves:
    truths:
      - The prototype owns first-class operator thread data instead of deriving ephemeral thread seeds inside the UI.
      - Thread identity is topic-based, with goal/stream/phase/plan carried as metadata instead of becoming the thread key.
      - The thread lifecycle is explicitly modeled as `pending`, `in-progress`, `waiting`, `silent`, and `resolved`.
      - Quick actions and freeform replies both operate through the same thread model.
      - Every new thread starts with a structured first Agent message that satisfies the locked context contract.
    artifacts:
      - /Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts
      - /Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx
      - /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts
      - /Users/realizer/Code/hopi/omc-prototype/src/prototype/scenario.ts
      - /Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts
    key_links:
      - The new message panel cannot scale until threads become real domain objects instead of render-time view objects.
      - The inbox and active-thread UI should be built on top of a stable thread model, not the other way around.
      - Lifecycle state is a product rule locked by context and must exist before UI grouping or default selection can be correct.
---

<objective>
Define the operator-thread domain and lifecycle that the new OMC message panel will run on.

Purpose: replace the current approval/risk/message patchwork with first-class thread, message, context, and lifecycle state.
Output: stable prototype thread types, store transitions, selector helpers, and structured first-message generation.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/realizer/Code/hopi/docs/design/omc-phase-01-context.md
@/Users/realizer/Code/hopi/docs/design/omc-planning-seed/01-CONTEXT.md
@/Users/realizer/Code/hopi/docs/design/omc-planning-seed/01-RESEARCH.md
@/Users/realizer/Code/hopi/docs/design/omc-planning-seed/01-VALIDATION.md
@/Users/realizer/Code/hopi/docs/design/omc-planning-seed/REQUIREMENTS.md
@/Users/realizer/Code/hopi/AGENTS.md
@/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts
@/Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx
@/Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts
@/Users/realizer/Code/hopi/omc-prototype/src/prototype/scenario.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce first-class operator thread, message, context, and lifecycle types</name>
  <files>/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts, /Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts</files>
  <read_first>/Users/realizer/Code/hopi/AGENTS.md, /Users/realizer/Code/hopi/docs/design/omc-phase-01-context.md, /Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts</read_first>
  <action>Define new prototype domain types for the message-first shell. Add explicit types such as `OperatorThread`, `OperatorMessage`, `ThreadLifecycleState`, `ThreadContextRef`, and `QuickActionSpec`. Keep thread state aligned to the locked lifecycle: `pending`, `in-progress`, `waiting`, `silent`, and `resolved`. Model goal/stream/phase/plan references as attached context metadata, not as the thread identity itself. Put thread derivation helpers or base factories in a dedicated `prototype/threads.ts` module so the UI stops building thread identity inline.</action>
  <verify>Run `cd /Users/realizer/Code/hopi && bun run typecheck:omc-prototype` after the new thread domain types compile cleanly.</verify>
  <acceptance_criteria>
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts` contains `OperatorThread`
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts` contains `ThreadLifecycleState`
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts` contains `pending`
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/types.ts` contains `silent`
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts` exists
  </acceptance_criteria>
  <done>The prototype has a stable thread domain independent from the right-rail component implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the store to persist per-thread state, messages, and structured first Agent messages</name>
  <files>/Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx, /Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts, /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts</files>
  <read_first>/Users/realizer/Code/hopi/AGENTS.md, /Users/realizer/Code/hopi/docs/design/omc-planning-seed/01-RESEARCH.md, /Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx, /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts</read_first>
  <action>Refactor the prototype store so chat data is per-thread rather than one flat array. Add selectors and reducer logic for: stable active-thread selection, unread/read markers, lifecycle transitions, quick-action application, and reopening a thread when new evidence invalidates an old decision. Generate the first Agent message for each new thread from structured data so approval, risk, direction, and status threads all include the required context: current status, background, why-now, suggested actions, and freeform invitation.</action>
  <verify>Run `cd /Users/realizer/Code/hopi && bun run typecheck:omc-prototype && bun run build:omc-prototype` after the store compiles and the scenario still builds.</verify>
  <acceptance_criteria>
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx` contains per-thread message storage rather than only one flat `chatMessages` array
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx` contains lifecycle transitions for `waiting` and `resolved`
    - `/Users/realizer/Code/hopi/omc-prototype/src/prototype/store.tsx` contains active-thread selection logic based on unresolved priority
    - structured first Agent messages exist for approval, risk, direction, and status threads
    - approval-thread first messages include confirm and defer consequences
    - risk-thread first messages include continue-silently and change-direction consequences
    - first Agent messages expose visible goal, stream, phase-or-plan, and impact metadata
  </acceptance_criteria>
  <done>The prototype store now owns the operator inbox semantics instead of leaving them to UI heuristics.</done>
</task>

<task type="auto">
  <name>Task 3: Make scenario and presentation layers thread-aware without making the canvas own the thread identity</name>
  <files>/Users/realizer/Code/hopi/omc-prototype/src/prototype/scenario.ts, /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts, /Users/realizer/Code/hopi/omc-prototype/src/prototype/threads.ts</files>
  <read_first>/Users/realizer/Code/hopi/AGENTS.md, /Users/realizer/Code/hopi/docs/design/omc-phase-01-context.md, /Users/realizer/Code/hopi/omc-prototype/src/prototype/scenario.ts, /Users/realizer/Code/hopi/omc-prototype/src/prototype/presenter.ts</read_first>
  <action>Update scenario- and presenter-level helpers so the prototype can derive operator topics from goals, approvals, risks, and direction changes without tying inbox membership to the current route. Build lightweight thread view models that expose title, preview, severity/tone, context refs, and suggested actions, but keep the actual thread lifecycle in the store. Ensure the presenter layer can feed both the future inbox rows and the first Agent message contract cleanly.</action>
  <verify>Run `cd /Users/realizer/Code/hopi && bun run typecheck:omc-prototype && bun run build:omc-prototype` after the presentation layer compiles and existing routes still build.</verify>
  <acceptance_criteria>
    - route context is no longer the source of truth for which threads exist
    - presenter helpers expose enough metadata for thread rows and first-message payloads
    - goals, approvals, and risks can all map to topic threads while keeping goal/stream/plan as metadata
  </acceptance_criteria>
  <done>The prototype has a stable bridge from scenario data into operator-thread topics without encoding thread logic in the UI layer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd /Users/realizer/Code/hopi && bun run typecheck:omc-prototype`
- [ ] `cd /Users/realizer/Code/hopi && bun run build:omc-prototype`
- [ ] Threads are first-class store objects with lifecycle `pending / in-progress / waiting / silent / resolved`
- [ ] Structured first Agent messages exist for new operator topics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The prototype owns a real operator-thread domain instead of ephemeral thread seeds
- Thread lifecycle is explicit and aligned with the locked Phase 1 context
</success_criteria>

<output>
After completion, create `/Users/realizer/Code/hopi/docs/design/omc-planning-seed/01-01-SUMMARY.md`
</output>
