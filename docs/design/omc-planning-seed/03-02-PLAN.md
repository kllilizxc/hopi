---
phase: 03-review-and-merge-cockpit
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - hub/src/sync/omc/reviewController.ts
  - hub/src/sync/omc/reviewPacket.ts
  - hub/src/sync/omc/runtimeStore.ts
  - hub/src/sync/omc/events.ts
  - hub/src/web/routes/omc.ts
  - hub/src/web/routes/omc.test.ts
  - hub/src/sync/omc/reviewController.test.ts
autonomous: true
requirements:
  - OMC-05
  - OMC-06
must_haves:
    truths:
      - OMC merge approval executes a real worktree merge through existing git RPCs instead of ad hoc shell behavior.
      - Merge success, merge-blocked failure, and merge conflict are represented as explicit OMC runtime outcomes.
      - Ordinary merge failures remain in `Review` and do not silently jump back into autonomous execution.
      - Conflicts stay in `Review` as `merge-blocked: conflict` and surface conflict files plus takeover linkage.
      - Merge execution remains plan-level and does not depend on task/project IDs or legacy task merge state.
    artifacts:
      - hub/src/sync/omc/reviewController.ts
      - hub/src/web/routes/omc.ts
      - hub/src/web/routes/omc.test.ts
      - hub/src/sync/omc/reviewController.test.ts
    key_links:
      - The user explicitly rejected automatic state-jumping repair after merge failure; the backend must preserve review posture.
      - Conflicts need special handling and evidence, not generic merge-failure treatment.
      - Existing git merge RPCs already exist, so OMC should stand on them directly rather than inventing another merge transport.
---

<objective>
Implement the real OMC merge approval path and conflict-aware review handling.

Purpose: turn review approval into a real merge action while preserving the user-chosen operator posture for merge failures and conflicts.
Output: OMC review/merge controller, merge approval execution, blocked/conflict handling, and route/test coverage for merge outcomes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/03-CONTEXT.md
@docs/design/omc-planning-seed/03-RESEARCH.md
@docs/design/omc-planning-seed/03-VALIDATION.md
@docs/design/omc-planning-seed/03-01-PLAN.md
@AGENTS.md
@hub/src/sync/rpcGateway.ts
@hub/src/sync/syncEngine.ts
@hub/src/sync/omc/runtimeStore.ts
@hub/src/sync/omc/loopController.ts
@hub/src/sync/omc/reviewPacket.ts
@hub/src/web/routes/omc.ts
@hub/src/web/routes/tasks.ts
@cli/src/modules/common/handlers/git.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an OMC review/merge controller that executes merge approval through existing git RPCs</name>
  <files>hub/src/sync/omc/reviewController.ts, hub/src/web/routes/omc.ts, hub/src/sync/omc/runtimeStore.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, docs/design/omc-planning-seed/03-RESEARCH.md, hub/src/sync/syncEngine.ts, hub/src/sync/rpcGateway.ts, hub/src/web/routes/tasks.ts, hub/src/web/routes/omc.ts</read_first>
  <action>Create `hub/src/sync/omc/reviewController.ts` with a concrete `OmcReviewController` that owns merge approval behavior separately from `OmcLoopController`. The merge-approval flow must require these concrete preconditions before calling the merge RPC: `runtime.column === "Review"`, `runtime.reviewApprovedAt != null`, `runtime.mergeStatus !== "merged"`, no active running attempt for the plan, and a resolved `targetBranch`. Use `engine.gitMergeWorktreeState(sessionId, { targetBranch })` to validate state before execution, then call `engine.gitMergeWorktree(sessionId, { targetBranch, commitMessage: \`OMC merge ${plan.planKey}: ${plan.planTitle}\`, strategy: "merge_commit" })` to perform the merge. Update OMC runtime fields on success with `mergeStatus: "merged"`, `mergeApprovedAt`, `doneAt`, and board column `Done`.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts && bun run typecheck:hub` after merge execution paths compile and test cleanly.</verify>
  <acceptance_criteria>
    - `hub/src/sync/omc/reviewController.ts` contains `class OmcReviewController`
    - `hub/src/sync/omc/reviewController.ts` contains `gitMergeWorktreeState`
    - `hub/src/sync/omc/reviewController.ts` contains `gitMergeWorktree`
    - `hub/src/sync/omc/reviewController.ts` contains `OMC merge `
    - `hub/src/web/routes/omc.ts` contains `plans/:planKey/merge/approve`
  </acceptance_criteria>
  <done>OMC can perform an actual merge approval from review state using existing git merge RPCs.</done>
</task>

<task type="auto">
  <name>Task 2: Keep merge failures in Review and treat conflicts as a dedicated conflict state</name>
  <files>hub/src/sync/omc/reviewController.ts, hub/src/sync/omc/runtimeStore.ts, hub/src/sync/omc/events.ts, hub/src/web/routes/omc.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, hub/src/web/routes/tasks.ts, hub/src/sync/omc/runtimeStore.ts, hub/src/sync/omc/events.ts</read_first>
  <action>Implement explicit merge-failure handling inside `OmcReviewController` rather than falling back to loop retry logic. For non-conflict merge failures, keep the plan in `Review`, set `mergeStatus: "blocked"`, persist `mergeBlockedReason`, and append merge evidence describing the failure. For conflict results, keep the plan in `Review`, set `mergeStatus: "conflict"`, persist `mergeBlockedReason`, record the conflict file list in evidence payload, and return takeover linkage based on the same worktree/session when available. Do not auto-call `retryPlan`, `resumePlan`, or `takeoverPlan` from merge failure paths.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts && bun run typecheck:hub` after merge-blocked and conflict paths are covered by tests.</verify>
  <acceptance_criteria>
    - `hub/src/sync/omc/reviewController.ts` contains `mergeStatus: 'blocked'` or `mergeStatus: \"blocked\"`
    - `hub/src/sync/omc/reviewController.ts` contains `mergeStatus: 'conflict'` or `mergeStatus: \"conflict\"`
    - `hub/src/sync/omc/reviewController.ts` contains `conflictFiles`
    - `hub/src/sync/omc/reviewController.ts` contains `sessionUrl`
    - `hub/src/web/routes/omc.ts` returns merge-blocked/conflict information from merge approval responses
  </acceptance_criteria>
  <done>Merge failures preserve the review gate and conflicts become a distinct OMC control-plane state.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused merge outcome tests for success, blocked failure, and conflict</name>
  <files>hub/src/sync/omc/reviewController.test.ts, hub/src/web/routes/omc.test.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-VALIDATION.md, hub/src/web/routes/omc.test.ts</read_first>
  <action>Add targeted tests that prove OMC merge approval can succeed, can stay review-blocked on non-conflict failure, and can surface conflict files plus takeover linkage on conflict. Keep these tests OMC-scoped and simulate git RPC responses directly rather than relying on the full task/worktree route stack.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/reviewController.test.ts` after the merge outcome tests are added.</verify>
  <acceptance_criteria>
    - `hub/src/sync/omc/reviewController.test.ts` contains a merge success case
    - `hub/src/sync/omc/reviewController.test.ts` contains a merge blocked case
    - `hub/src/sync/omc/reviewController.test.ts` contains a conflict case
    - `hub/src/web/routes/omc.test.ts` covers `merge/approve`
  </acceptance_criteria>
  <done>Phase 3 has a regression harness for the three core merge outcomes before cockpit UI work builds on top.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts`
- [ ] `cd hopi && bun run typecheck:hub`
- [ ] Merge success can move one review-approved plan to a merged/done posture
- [ ] Merge failure can stay in `Review` as `merge-blocked`
- [ ] Merge conflict can stay in `Review` as `merge-blocked: conflict` with conflict-file evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC can execute merge approval through existing git merge primitives
- Merge failures and conflicts preserve the explicit review posture chosen by the user
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/03-02-SUMMARY.md
</output>
