---
phase: 04-program-bootstrap-and-planning-attach
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - OMC-client/src/api/client.tsx
  - OMC-client/src/router.tsx
  - OMC-client/src/components/PlanBoard.tsx
  - OMC-client/src/components/PlanningBootstrapPanel.tsx
  - OMC-client/src/index.css
autonomous: true
requirements:
  - OMC-03
  - OMC-07
  - OMC-10
must_haves:
    truths:
      - Planning-missing state is actionable, not a dead-end warning.
      - Users can choose `Attach existing planning` or `Create planning seed` from the UI.
      - Creating a seed leads into a planning-first posture with clear next steps instead of immediate execution controls.
      - The product makes repo-local `.planning/*` creation legible after bootstrap.
    artifacts:
      - OMC-client/src/components/PlanBoard.tsx
      - OMC-client/src/components/PlanningBootstrapPanel.tsx
      - OMC-client/src/api/client.tsx
      - OMC-client/src/router.tsx
    key_links:
      - The user explicitly wants the `No plans found yet` dead end removed.
      - Phase 4 bootstrap is intentionally lightweight, so the UX must emphasize next planning steps rather than auto-generating execution artifacts.
      - The repo-local `.planning/*` result should be visible and understandable from the UI.
---

<objective>
Replace OMC’s planning-missing dead end with a planning-first bootstrap experience.

Purpose: make repos like `PersonalQuant` usable the moment they are attached, even before any formal GSD plan exists.
Output: actionable empty-state UI, attach-planning and create-seed actions, and planning-first guidance after bootstrap.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/04-CONTEXT.md
@docs/design/omc-planning-seed/04-RESEARCH.md
@docs/design/omc-planning-seed/04-VALIDATION.md
@docs/design/omc-planning-seed/04-01-PLAN.md
@docs/design/omc-planning-seed/04-02-PLAN.md
@AGENTS.md
@OMC-client/src/api/client.tsx
@OMC-client/src/router.tsx
@OMC-client/src/components/PlanBoard.tsx
@OMC-client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the planning-empty state into an actionable bootstrap panel</name>
  <files>OMC-client/src/components/PlanBoard.tsx, OMC-client/src/components/PlanningBootstrapPanel.tsx, OMC-client/src/index.css</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/omc-planning-seed/04-RESEARCH.md, OMC-client/src/components/PlanBoard.tsx, OMC-client/src/index.css</read_first>
  <action>Create `OMC-client/src/components/PlanningBootstrapPanel.tsx` and replace the current passive `No plans found yet` guidance with explicit attach/bootstrap actions. The panel must clearly present `Attach existing planning` and `Create planning seed`, explain why the repo is not execution-ready yet, and keep the tone planning-first rather than error-first.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the new bootstrap panel compiles and builds successfully.</verify>
  <acceptance_criteria>
    - `OMC-client/src/components/PlanBoard.tsx` no longer only suggests setting `HOPI_OMC_PLANNING_ROOT`
    - `OMC-client/src/components/PlanningBootstrapPanel.tsx` exists
    - `OMC-client/src/components/PlanningBootstrapPanel.tsx` contains `Attach existing planning`
    - `OMC-client/src/components/PlanningBootstrapPanel.tsx` contains `Create planning seed`
  </acceptance_criteria>
  <done>The planning-missing state becomes actionable instead of a dead end.</done>
</task>

<task type="auto">
  <name>Task 2: Wire attach-planning and create-seed actions into the program flow</name>
  <files>OMC-client/src/api/client.tsx, OMC-client/src/router.tsx, OMC-client/src/components/PlanningBootstrapPanel.tsx</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, OMC-client/src/api/client.tsx, OMC-client/src/router.tsx</read_first>
  <action>Wire the empty-state actions to the real backend attach/bootstrap APIs. `Attach existing planning` should let the user provide or confirm a planning root and refresh the program into a planning-aware state. `Create planning seed` should call the backend seed action and refresh the program without requiring a manual page reload.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after the empty-state action flow compiles and builds cleanly.</verify>
  <acceptance_criteria>
    - `OMC-client/src/components/PlanningBootstrapPanel.tsx` calls attach-planning action(s)
    - `OMC-client/src/components/PlanningBootstrapPanel.tsx` calls create-seed action(s)
    - the program refreshes into post-bootstrap state without manual reload
  </acceptance_criteria>
  <done>Users can recover from planning-missing state entirely inside OMC-client.</done>
</task>

<task type="auto">
  <name>Task 3: Show planning-first post-bootstrap guidance instead of execution-first controls</name>
  <files>OMC-client/src/router.tsx, OMC-client/src/components/PlanningBootstrapPanel.tsx, OMC-client/src/index.css</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/04-CONTEXT.md, docs/design/factory-mode-ralph-gsd.md, OMC-client/src/router.tsx, OMC-client/src/index.css</read_first>
  <action>After planning is attached or seeded, show clear next-step guidance that points the user toward planning artifacts and the next GSD step instead of foregrounding `Start plan`. Make the resulting state legible: which `.planning/*` files now exist, why the board may still be empty, and what the next action should be.</action>
  <verify>Run `cd hopi && bun run typecheck:omc && bun run build:omc` after post-bootstrap guidance compiles and builds cleanly.</verify>
  <acceptance_criteria>
    - post-bootstrap state explains the generated or attached planning files
    - post-bootstrap state emphasizes planning next steps over execution controls
    - repos like `PersonalQuant` no longer feel stuck after seed creation
  </acceptance_criteria>
  <done>Phase 4 ends in a clear planning-first posture instead of a confusing empty board.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck:omc`
- [ ] `cd hopi && bun run build:omc`
- [ ] Planning-missing state exposes attach/seed actions
- [ ] Creating a seed refreshes the program into planning-first guidance without manual reload
- [ ] The UI does not encourage immediate loop start right after seed creation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC no longer dead-ends when a repo has no planning
- The product posture after bootstrap is clearly planning-first
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/04-03-SUMMARY.md
</output>
