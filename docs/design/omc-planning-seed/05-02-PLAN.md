---
phase: 05-guided-planning-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - hub/src/sync/omc/planningRunController.ts
  - hub/src/sync/omc/planningRuntimeAdapter.ts
  - hub/src/sync/omc/planningPromptBuilder.ts
  - hub/src/sync/omc/planningAutomation.ts
  - hub/src/sync/syncEngine.ts
  - hub/src/web/routes/omc.ts
  - hub/src/web/routes/omc.test.ts
autonomous: true
requirements:
  - OMC-03
  - OMC-10
  - OMC-11
  - OMC-12
  - OMC-13
must_haves:
    truths:
      - OMC can start guided planning from a lightweight brief after seed creation.
      - Guided planning reuses existing session spawn/send-message substrate instead of inventing a second execution backend.
      - Completion is grounded in real planning artifacts appearing in `.planning/*`, especially `PLAN.md` cards.
      - Failures and retries are explicit program-level runtime outcomes, not silent bootstrap fallbacks.
    artifacts:
      - hub/src/sync/omc/planningRunController.ts
      - hub/src/sync/omc/planningRuntimeAdapter.ts
      - hub/src/sync/omc/planningPromptBuilder.ts
      - hub/src/sync/omc/planningAutomation.ts
      - hub/src/web/routes/omc.ts
      - hub/src/web/routes/omc.test.ts
    key_links:
      - The user wants “repo in, first plan out,” not “repo in, now go use the terminal.”
      - Phase 5 must solve the specific gap between seed files and the first real plan cards.
---

<objective>
Implement the backend guided-planning orchestration loop for OMC.

Purpose: turn a lightweight program brief into a real planning run that creates the first executable `PLAN.md` files.
Output: controller/service, runtime adapter, prompt builder, automation hooks, mutating APIs, and backend tests.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/05-CONTEXT.md
@docs/design/omc-planning-seed/05-RESEARCH.md
@docs/design/omc-planning-seed/05-01-PLAN.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@AGENTS.md
@hub/src/sync/omc/attemptRunner.ts
@hub/src/sync/omc/runtimeAdapter.ts
@hub/src/sync/omc/programBootstrap.ts
@hub/src/sync/syncEngine.ts
@hub/src/web/routes/omc.ts
@hub/src/web/routes/omc.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a Codex-first planning runtime adapter and prompt builder</name>
  <files>hub/src/sync/omc/planningRuntimeAdapter.ts, hub/src/sync/omc/planningPromptBuilder.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-RESEARCH.md, hub/src/sync/omc/runtimeAdapter.ts, hub/src/sync/omc/programBootstrap.ts</read_first>
  <action>Create a guided-planning runtime adapter and prompt builder that reuse OMC’s existing session spawn/send-message posture but target program-level planning continuation instead of plan execution. The prompt should consume a lightweight brief plus the seeded `.planning/*` files and aim explicitly at producing the first executable `PLAN.md` cards.</action>
  <verify>Run `bun run typecheck:hub` after the new runtime adapter and prompt builder compile cleanly.</verify>
  <acceptance_criteria>
    - planning runtime adapter exists
    - planning prompt builder exists
    - prompt contract is program-scoped and does not require an existing `planKey`
  </acceptance_criteria>
  <done>Guided planning has a deterministic runtime entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Add planning-run controller, automation, and mutating APIs</name>
  <files>hub/src/sync/omc/planningRunController.ts, hub/src/sync/omc/planningAutomation.ts, hub/src/sync/syncEngine.ts, hub/src/web/routes/omc.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-CONTEXT.md, docs/design/omc-planning-seed/05-RESEARCH.md, hub/src/sync/omc/loopAutomation.ts, hub/src/sync/syncEngine.ts, hub/src/web/routes/omc.ts</read_first>
  <action>Implement the lifecycle for starting, canceling, retrying, and observing guided-planning runs. Hook it into SyncEngine event handling so message/session updates can move the planning run forward or fail it, and mark completion only when real `PLAN.md` cards now exist under the program planning root.</action>
  <verify>Run `bun run typecheck:hub` after the controller/automation/APIs compile cleanly.</verify>
  <acceptance_criteria>
    - OMC exposes start/cancel/retry APIs for guided planning
    - guided-planning runs can be advanced or failed from realtime session events
    - completion checks actual planning artifacts rather than only trusting a session summary
  </acceptance_criteria>
  <done>OMC can now drive and observe a real guided-planning run on the backend.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused backend tests for success, failure, and plan emergence</name>
  <files>hub/src/web/routes/omc.test.ts, hub/src/sync/omc/planningAutomation.ts, hub/src/sync/omc/planningRunController.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/05-RESEARCH.md, hub/src/web/routes/omc.test.ts</read_first>
  <action>Add OMC-focused tests that prove: guided planning can start from a seed-only repo, failures are surfaced explicitly, and the system recognizes success only once the first executable `PLAN.md` cards appear.</action>
  <verify>Run `bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts` after the new tests are added.</verify>
  <acceptance_criteria>
    - backend tests cover guided-planning start
    - backend tests cover guided-planning failure
    - backend tests cover first-plan emergence as the success condition
  </acceptance_criteria>
  <done>Phase 5 backend orchestration has regression protection before UI wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run typecheck:hub`
- [ ] `bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts`
- [ ] Guided planning can create the first real `PLAN.md` cards from a seed-only repo
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC can orchestrate guided planning from bootstrap to plan-card emergence on the backend
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/05-02-SUMMARY.md
</output>
