---
phase: 03-review-and-merge-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/schemas.ts
  - shared/src/types.ts
  - hub/src/store/index.ts
  - hub/src/store/types.ts
  - hub/src/sync/omc/runtimeStore.ts
  - hub/src/sync/omc/events.ts
  - hub/src/sync/omc/reviewPacket.ts
  - hub/src/web/routes/omc.ts
  - hub/src/web/routes/omc.test.ts
autonomous: true
requirements:
  - OMC-05
  - OMC-06
must_haves:
    truths:
      - OMC has explicit review and merge contracts instead of inferring merge state from `reviewRequired` and evidence text alone.
      - Review approval, merge approval, merge-blocked state, and merge packet payloads are first-class OMC concepts in shared types and runtime storage.
      - OMC can build a merge packet from plan runtime, attempts, evidence, and worktree metadata before any merge is executed.
      - Review APIs exist for approval and explicit reopen actions without dropping into task-specific flows.
      - Review and merge updates have dedicated SSE event families so cockpit state does not depend on local refreshes alone.
    artifacts:
      - shared/src/schemas.ts
      - shared/src/types.ts
      - hub/src/sync/omc/runtimeStore.ts
      - hub/src/sync/omc/reviewPacket.ts
      - hub/src/sync/omc/events.ts
      - hub/src/web/routes/omc.ts
    key_links:
      - Phase 3 cannot have a real cockpit until review and merge are represented as explicit OMC runtime concepts.
      - The user explicitly chose decision-first merge packets and explicit reopen actions, so those contracts belong before merge execution and UI work.
      - Dedicated review/merge events are needed because OMC-client already relies on SSE invalidation for runtime freshness.
---

<objective>
Introduce the OMC review and merge packet contract so review state, merge readiness, and reopen decisions are explicit parts of the runtime.

Purpose: give Phase 3 a stable backend contract for human review and merge decisions before real merge execution or cockpit UI work begins.
Output: shared/runtime review+merge schema additions, merge packet builder, review APIs, and review/merge SSE events.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/design/omc-planning-seed/PROJECT.md
@docs/design/omc-planning-seed/ROADMAP.md
@docs/design/omc-planning-seed/STATE.md
@docs/design/omc-planning-seed/REQUIREMENTS.md
@docs/design/omc-planning-seed/03-CONTEXT.md
@docs/design/omc-planning-seed/03-RESEARCH.md
@docs/design/omc-planning-seed/03-VALIDATION.md
@AGENTS.md
@shared/src/schemas.ts
@shared/src/types.ts
@hub/src/store/index.ts
@hub/src/store/types.ts
@hub/src/sync/omc/runtimeStore.ts
@hub/src/sync/omc/events.ts
@hub/src/web/routes/omc.ts
@hub/src/web/routes/omc.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote review and merge state to first-class OMC shared/runtime contracts</name>
  <files>shared/src/schemas.ts, shared/src/types.ts, hub/src/store/index.ts, hub/src/store/types.ts, hub/src/sync/omc/runtimeStore.ts, hub/src/sync/omc/events.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, docs/design/omc-planning-seed/03-RESEARCH.md, shared/src/schemas.ts, hub/src/store/index.ts, hub/src/sync/omc/runtimeStore.ts</read_first>
  <action>Extend the OMC shared/runtime contract with explicit review and merge fields instead of relying only on `reviewRequired` and `mergeApprovedAt`. Add `OmcMergeStatusSchema` with the concrete values `idle`, `ready`, `merging`, `blocked`, `conflict`, and `merged`. Extend `OmcPlanRuntimeSchema` and runtime storage with `reviewApprovedAt`, `mergeStatus`, `mergeBlockedReason`, and `lastMergeAttemptAt`. Add dedicated review/merge event types named `omc-review-updated` and `omc-merge-updated`. Update SQLite schema/versioning, row mappings, and store upsert/read logic so these fields persist cleanly.</action>
  <verify>Run `cd hopi && bun run typecheck` after the new shared/runtime contracts and store schema changes compile across packages.</verify>
  <acceptance_criteria>
    - `shared/src/schemas.ts` contains `OmcMergeStatusSchema`
    - `shared/src/schemas.ts` contains `reviewApprovedAt`
    - `shared/src/schemas.ts` contains `mergeStatus`
    - `shared/src/schemas.ts` contains `mergeBlockedReason`
    - `hub/src/store/index.ts` contains new OMC runtime columns for the review/merge fields
    - `hub/src/sync/omc/events.ts` contains `omc-review-updated`
    - `hub/src/sync/omc/events.ts` contains `omc-merge-updated`
  </acceptance_criteria>
  <done>OMC review and merge state are durable, typed, and evented rather than implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Build a decision-first merge packet contract and review APIs</name>
  <files>hub/src/sync/omc/reviewPacket.ts, hub/src/web/routes/omc.ts, hub/src/sync/omc/runtimeStore.ts, shared/src/schemas.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-CONTEXT.md, docs/design/factory-mode-ralph-gsd.md, hub/src/web/routes/omc.ts, hub/src/sync/omc/runtimeStore.ts, hub/src/sync/omc/evidenceCollector.ts</read_first>
  <action>Create `hub/src/sync/omc/reviewPacket.ts` with a concrete `buildOmcMergePacket` function and a shared packet schema named `OmcMergePacketSchema`. The packet must include `phaseLabel`, `planKey`, `planTitle`, `targetBranch`, `sourceBranch`, `worktreePath`, `attemptCount`, `changedFilesSummary`, `checksSummary`, `completionSummary`, `warnings`, `blockers`, and `preconditions`. Add three OMC routes in `hub/src/web/routes/omc.ts`: `GET /omc/programs/:programId/plans/:planKey/merge-packet`, `POST /omc/programs/:programId/plans/:planKey/review/approve`, and `POST /omc/programs/:programId/plans/:planKey/review/reopen`. The reopen route must accept the concrete JSON body `{ \"action\": \"resume_loop\" | \"back_to_planning\" }` and update runtime state accordingly.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts && bun run typecheck:hub` after merge-packet and review-route coverage is in place.</verify>
  <acceptance_criteria>
    - `shared/src/schemas.ts` contains `OmcMergePacketSchema`
    - `hub/src/sync/omc/reviewPacket.ts` contains `buildOmcMergePacket`
    - `hub/src/web/routes/omc.ts` contains `plans/:planKey/merge-packet`
    - `hub/src/web/routes/omc.ts` contains `plans/:planKey/review/approve`
    - `hub/src/web/routes/omc.ts` contains `plans/:planKey/review/reopen`
    - `hub/src/web/routes/omc.ts` contains `resume_loop`
    - `hub/src/web/routes/omc.ts` contains `back_to_planning`
  </acceptance_criteria>
  <done>OMC can expose one merge-ready review payload and explicit review decisions without yet executing the merge.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for merge packet and review state transitions</name>
  <files>hub/src/web/routes/omc.test.ts, hub/src/sync/omc/reviewPacket.test.ts</files>
  <read_first>AGENTS.md, docs/design/omc-planning-seed/03-VALIDATION.md, hub/src/web/routes/omc.test.ts</read_first>
  <action>Add targeted hub tests that prove OMC can build an API-visible merge packet, approve review without merging, and reopen review back to `Running` or `Planning` depending on the posted action. Keep the tests OMC-focused and do not depend on unrelated task routes or full hub-suite cleanliness.</action>
  <verify>Run `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/reviewPacket.test.ts` after the new review/merge packet tests are added.</verify>
  <acceptance_criteria>
    - `hub/src/sync/omc/reviewPacket.test.ts` contains a merge packet case
    - `hub/src/web/routes/omc.test.ts` contains `review/approve`
    - `hub/src/web/routes/omc.test.ts` contains `review/reopen`
    - `hub/src/web/routes/omc.test.ts` contains `merge-packet`
  </acceptance_criteria>
  <done>Phase 3 has a regression harness for review packet generation and review state transitions before merge execution work starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd hopi && bun run typecheck`
- [ ] `cd hopi && bun test hub/src/web/routes/omc.test.ts hub/src/sync/omc/*.test.ts`
- [ ] OMC can return a decision-first merge packet for one reviewable plan
- [ ] Review approval and explicit reopen actions are API-visible and durable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- OMC review/merge state is explicit and typed
- OMC can expose merge-packet and review-decision APIs without inferring everything from generic evidence text
</success_criteria>

<output>
After completion, create docs/design/omc-planning-seed/03-01-SUMMARY.md
</output>
